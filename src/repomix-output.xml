This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
Avalonia.Markup.Declarative.sln
Avalonia.Markup.Declarative.SourceGenerator/Avalonia.Markup.Declarative.SourceGenerator.csproj
Avalonia.Markup.Declarative.SourceGenerator/AvaloniaEventExtensionsGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/AvaloniaPropertyExtensionsGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/AttachedPropertyExtensionInfo.cs
Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/EventExtensionInfo.cs
Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/IMemberExtensionInfo.cs
Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/PropertyExtensionInfo.cs
Avalonia.Markup.Declarative.SourceGenerator/ExternalAssemblyExtensionsGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/ExternalGeneratorHost.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/AttachedPropertySetterGenerator/AttachedPropertyBindFromExpressionSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/AttachedPropertySetterGenerator/AttachedPropertyValueSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/EventGenerators/ActionToEventGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/ExtensionGeneratorBase.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/ExtensionGroupGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/AvaloniaPropertyBindSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/BindFromExpressionSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/ValueOverloadsSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/ValueSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/BindFromExpressionStyleSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/ValueOverloadsStyleSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/ValueStyleSetterGenerator.cs
Avalonia.Markup.Declarative.SourceGenerator/MarkupTypeHelpers.cs
Avalonia.Markup.Declarative.SourceGenerator/SymbolUtilities.cs
Avalonia.Markup.Declarative.Tests/Avalonia.Markup.Declarative.Tests.csproj
Avalonia.Markup.Declarative.Tests/AvaloniaTestBase.cs
Avalonia.Markup.Declarative.Tests/BindingTests/BrushBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/CommandBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/ExpressionBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/ListBoxBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/NumericUpDownBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/StyleBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/TemplateBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/TreeViewBindingTests.cs
Avalonia.Markup.Declarative.Tests/BindingTests/ViewBaseBindingTests.cs
Avalonia.Markup.Declarative.Tests/ControlsTests/AttachedPropertySetterTests.cs
Avalonia.Markup.Declarative.Tests/ControlsTests/ExternalPropertySetTests.cs
Avalonia.Markup.Declarative.Tests/ControlsTests/ModalHostTests.cs
Avalonia.Markup.Declarative.Tests/ControlsTests/MyCustomControlBindingTests.cs
Avalonia.Markup.Declarative.Tests/GlobalSuppressions.cs
Avalonia.Markup.Declarative.Tests/HelpersTests/SelectorParserHelperTests.cs
Avalonia.Markup.Declarative/Avalonia.Markup.Declarative.csproj
Avalonia.Markup.Declarative/CommonExtensions/AppBuilderExtensions.cs
Avalonia.Markup.Declarative/CommonExtensions/BindingExtensions.cs
Avalonia.Markup.Declarative/CommonExtensions/ControlEventExtensions.cs
Avalonia.Markup.Declarative/CommonExtensions/ConverterExtensions.cs
Avalonia.Markup.Declarative/ControlPropertyExtensions.cs
Avalonia.Markup.Declarative/FuncView.cs
Avalonia.Markup.Declarative/GenerateMarkupExtensionsForAssemblyAttribute.cs
Avalonia.Markup.Declarative/Helpers/SelectorParserHelper.cs
Avalonia.Markup.Declarative/HotReloadManager.cs
Avalonia.Markup.Declarative/IComponentControlFactory.cs
Avalonia.Markup.Declarative/Icon.png
Avalonia.Markup.Declarative/IDeclarativeViewBase.cs
Avalonia.Markup.Declarative/IReloadable.cs
Avalonia.Markup.Declarative/README.md
Avalonia.Markup.Declarative/readme.txt
Avalonia.Markup.Declarative/StylePropertyExtensions.cs
Avalonia.Markup.Declarative/Styles/Style.cs
Avalonia.Markup.Declarative/Styles/StyleBuilder.cs
Avalonia.Markup.Declarative/Styles/StyleGroup.cs
Avalonia.Markup.Declarative/ViewBase.cs
Avalonia.Markup.Declarative/ViewFactory.cs
Avalonia.Markup.Declarative/WindowExtensions.cs
Samples/AvaloniaMarkupSample/App.cs
Samples/AvaloniaMarkupSample/Assets/teapot.bin
Samples/AvaloniaMarkupSample/avalonia-logo.ico
Samples/AvaloniaMarkupSample/AvaloniaMarkupSample.csproj
Samples/AvaloniaMarkupSample/CommonSamples/ItemControlSampleView.cs
Samples/AvaloniaMarkupSample/CommonSamples/MyCustomControl.cs
Samples/AvaloniaMarkupSample/CommonSamples/MyCustomTemplatedControl.cs
Samples/AvaloniaMarkupSample/CommonSamples/StylesSampleView.cs
Samples/AvaloniaMarkupSample/CommonSamples/TreeViewSampleView.cs
Samples/AvaloniaMarkupSample/MainView.cs
Samples/AvaloniaMarkupSample/MvvmSample/MvvmSampleView.cs
Samples/AvaloniaMarkupSample/MvvmSample/MvvmSampleViewModel.cs
Samples/AvaloniaMarkupSample/Properties/launchSettings.json
Samples/AvaloniaMarkupSample/StaticResources.cs
Samples/DeclarativeComponentTemplate/.gitignore
Samples/DeclarativeComponentTemplate/App.cs
Samples/DeclarativeComponentTemplate/CounterComponent.cs
Samples/DeclarativeComponentTemplate/DeclarativeComponentTemplate.csproj
Samples/DeclarativeComponentTemplate/MainView.cs
Samples/ReactiveSample/App.cs
Samples/ReactiveSample/AppViewLocator.cs
Samples/ReactiveSample/avalonia-logo.ico
Samples/ReactiveSample/ControlExtensions.Generated/ColorPaletteResources_MarkupExtensions.g.cs
Samples/ReactiveSample/ControlExtensions.Generated/FluentTheme_MarkupExtensions.g.cs
Samples/ReactiveSample/ControlExtensions.Generated/RoutedViewHost_MarkupExtensions.g.cs
Samples/ReactiveSample/ControlExtensions.Generated/ViewModelViewHost_MarkupExtensions.g.cs
Samples/ReactiveSample/Properties/launchSettings.json
Samples/ReactiveSample/ReactiveExtensions.cs
Samples/ReactiveSample/ReactiveSample.csproj
Samples/ReactiveSample/ViewModels/MainViewModel.cs
Samples/ReactiveSample/ViewModels/PageViewModel1.cs
Samples/ReactiveSample/ViewModels/PageViewModel2.cs
Samples/ReactiveSample/Views/MainView.cs
Samples/ReactiveSample/Views/PageView1.cs
Samples/ReactiveSample/Views/PageView2.cs
Samples/ReactiveSample/Views/ReactiveViewBase.cs
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="Avalonia.Markup.Declarative.sln">
Microsoft Visual Studio Solution File, Format Version 12.00
# Visual Studio Version 17
VisualStudioVersion = 17.1.31911.260
MinimumVisualStudioVersion = 10.0.40219.1
Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "Avalonia.Markup.Declarative", "Avalonia.Markup.Declarative\Avalonia.Markup.Declarative.csproj", "{5B5C0172-0B35-470A-9A65-4679E6028454}"
EndProject
Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "AvaloniaMarkupSample", "Samples\AvaloniaMarkupSample\AvaloniaMarkupSample.csproj", "{612D87E7-CF30-4E08-A7C3-AF4482924EBC}"
EndProject
Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "DeclarativeComponentTemplate", "Samples\DeclarativeComponentTemplate\DeclarativeComponentTemplate.csproj", "{43B91641-30D5-448A-A996-16DFB85B7262}"
EndProject
Project("{9A19103F-16F7-4668-BE54-9A1E7A4F7556}") = "Avalonia.Markup.Declarative.SourceGenerator", "Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj", "{66115820-E3EB-45E2-A45C-AD79652CA3A8}"
EndProject
Project("{2150E333-8FDC-42A3-9474-1A3956D46DE8}") = "Samples", "Samples", "{0A80E8C8-B71D-4616-AF4F-B2C70536E61F}"
EndProject
Project("{2150E333-8FDC-42A3-9474-1A3956D46DE8}") = "Global", "Global", "{2495724C-3E46-4375-B547-AFC84BCAEFC5}"
	ProjectSection(SolutionItems) = preProject
		..\Directory.Build.props = ..\Directory.Build.props
		..\global.json = ..\global.json
	EndProjectSection
EndProject
Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "Avalonia.Markup.Declarative.Tests", "Avalonia.Markup.Declarative.Tests\Avalonia.Markup.Declarative.Tests.csproj", "{A8D7B9DD-5455-49DA-B148-514143FBF483}"
EndProject
Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "ReactiveSample", "Samples\ReactiveSample\ReactiveSample.csproj", "{6CB0BB7F-7699-E33D-C315-25389E3518CC}"
EndProject
Global
	GlobalSection(SolutionConfigurationPlatforms) = preSolution
		Debug|Any CPU = Debug|Any CPU
		Release|Any CPU = Release|Any CPU
	EndGlobalSection
	GlobalSection(ProjectConfigurationPlatforms) = postSolution
		{5B5C0172-0B35-470A-9A65-4679E6028454}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{5B5C0172-0B35-470A-9A65-4679E6028454}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{5B5C0172-0B35-470A-9A65-4679E6028454}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{5B5C0172-0B35-470A-9A65-4679E6028454}.Release|Any CPU.Build.0 = Release|Any CPU
		{612D87E7-CF30-4E08-A7C3-AF4482924EBC}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{612D87E7-CF30-4E08-A7C3-AF4482924EBC}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{612D87E7-CF30-4E08-A7C3-AF4482924EBC}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{612D87E7-CF30-4E08-A7C3-AF4482924EBC}.Release|Any CPU.Build.0 = Release|Any CPU
		{43B91641-30D5-448A-A996-16DFB85B7262}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{43B91641-30D5-448A-A996-16DFB85B7262}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{43B91641-30D5-448A-A996-16DFB85B7262}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{43B91641-30D5-448A-A996-16DFB85B7262}.Release|Any CPU.Build.0 = Release|Any CPU
		{66115820-E3EB-45E2-A45C-AD79652CA3A8}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{66115820-E3EB-45E2-A45C-AD79652CA3A8}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{66115820-E3EB-45E2-A45C-AD79652CA3A8}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{66115820-E3EB-45E2-A45C-AD79652CA3A8}.Release|Any CPU.Build.0 = Release|Any CPU
		{A8D7B9DD-5455-49DA-B148-514143FBF483}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{A8D7B9DD-5455-49DA-B148-514143FBF483}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{A8D7B9DD-5455-49DA-B148-514143FBF483}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{A8D7B9DD-5455-49DA-B148-514143FBF483}.Release|Any CPU.Build.0 = Release|Any CPU
		{6CB0BB7F-7699-E33D-C315-25389E3518CC}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{6CB0BB7F-7699-E33D-C315-25389E3518CC}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{6CB0BB7F-7699-E33D-C315-25389E3518CC}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{6CB0BB7F-7699-E33D-C315-25389E3518CC}.Release|Any CPU.Build.0 = Release|Any CPU
	EndGlobalSection
	GlobalSection(SolutionProperties) = preSolution
		HideSolutionNode = FALSE
	EndGlobalSection
	GlobalSection(NestedProjects) = preSolution
		{612D87E7-CF30-4E08-A7C3-AF4482924EBC} = {0A80E8C8-B71D-4616-AF4F-B2C70536E61F}
		{43B91641-30D5-448A-A996-16DFB85B7262} = {0A80E8C8-B71D-4616-AF4F-B2C70536E61F}
		{6CB0BB7F-7699-E33D-C315-25389E3518CC} = {0A80E8C8-B71D-4616-AF4F-B2C70536E61F}
	EndGlobalSection
	GlobalSection(ExtensibilityGlobals) = postSolution
		SolutionGuid = {A52A1772-62A5-484B-8A6D-09674800867E}
	EndGlobalSection
EndGlobal
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Avalonia.Markup.Declarative.SourceGenerator.csproj">
<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>netstandard2.0</TargetFramework>
		<LangVersion>latest</LangVersion>
		<ImplicitUsings>enable</ImplicitUsings>
		<GeneratePackageOnBuild>False</GeneratePackageOnBuild>
		<EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Avalonia" Version="12.0.0" />
		<PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="5.3.0" PrivateAssets="all" />
		<PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="5.3.0" PrivateAssets="all" />
	</ItemGroup>

</Project>
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/AvaloniaEventExtensionsGenerator.cs">
using System.Diagnostics;
using System.Text;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.CSharp.Syntax;
using Microsoft.CodeAnalysis.Text;
using static Avalonia.Markup.Declarative.SourceGenerator.MarkupTypeHelpers;

namespace Avalonia.Markup.Declarative.SourceGenerator;

[Generator]
public class AvaloniaEventExtensionsGenerator : IIncrementalGenerator
{
    public void Initialize(IncrementalGeneratorInitializationContext context)
    {
#if DEBUG
        if (!Debugger.IsAttached)
        {
            //Debugger.Launch();
        }
#endif
        Debug.WriteLine("Initialize AvaloniaEventExtensionsGenerator code generator");

        var classDeclarations = context.SyntaxProvider
            .CreateSyntaxProvider(
                predicate: static (s, _) => s is ClassDeclarationSyntax,
                transform: static (ctx, _) => GetSemanticTarget(ctx))
            .Where(static c => c is not null);

        context.RegisterSourceOutput(classDeclarations,
            static (spc, classDecl) => GenerateSource(spc, classDecl!));
    }

    private static ClassDeclarationSyntax? GetSemanticTarget(GeneratorSyntaxContext context)
    {
        var classDecl = (ClassDeclarationSyntax)context.Node;
        var symbol = context.SemanticModel.GetDeclaredSymbol(classDecl);
        if (symbol is INamedTypeSymbol typeSymbol && IsAvaloniaMarkupView(typeSymbol))
        {
            return classDecl;
        }
        return null;
    }

    private static void GenerateSource(SourceProductionContext context, ClassDeclarationSyntax classDecl)
    {
        var root = classDecl.SyntaxTree.GetRoot();

        var ns = root
            .DescendantNodes()
            .FirstOrDefault(x => x is BaseNamespaceDeclarationSyntax) as BaseNamespaceDeclarationSyntax;

        var typeNamespace = ns?.Name.ToString() ?? string.Empty;

        var sb = new StringBuilder();

        sb.AppendLine("#nullable enable");
        sb.AppendLine("// Auto-generated code");
        sb.AppendLine("using System;");
        sb.AppendLine("using System.Runtime.CompilerServices;");

        if (root is CompilationUnitSyntax compilationUnit)
        {
            foreach (var usingDirective in compilationUnit.Usings)
            {
                sb.AppendLine(usingDirective.ToString());
            }
        }

        if (!string.IsNullOrWhiteSpace(typeNamespace))
            sb.AppendLine($"using {typeNamespace};");

        // Build the nested-qualified name (e.g., Outer.Inner for a nested class) so we can reference nested types.
        static string GetNestedQualifiedName(ClassDeclarationSyntax cls)
        {
            var names = new System.Collections.Generic.List<string>();
            SyntaxNode? node = cls;
            while (node is ClassDeclarationSyntax cds)
            {
                names.Insert(0, cds.Identifier.ToString());
                node = node.Parent;
            }
            return string.Join(".", names);
        }

        // The name to use in method signatures (within the containing namespace scope via using above)
        var nestedQualifiedTypeName = GetNestedQualifiedName(classDecl);

        // Create a unique and valid extension class/file name that includes namespace and nesting
        var fullTypePathForId = (string.IsNullOrWhiteSpace(typeNamespace) ? string.Empty : typeNamespace + ".") + nestedQualifiedTypeName;
        var safeId = fullTypePathForId
            .Replace('.', '_')
            .Replace('+', '_'); // just in case

        sb.AppendLine("namespace Avalonia.Markup.Declarative;");
        sb.AppendLine($"public static partial class {safeId}EventExtensions");
        sb.AppendLine("{");

        var processedMembersCount = 0;
        foreach (var member in classDecl.Members)
        {
            if (member is not EventFieldDeclarationSyntax @event)
                continue;

            var extensionString = GetEventExtension(nestedQualifiedTypeName, @event);
            if (string.IsNullOrWhiteSpace(extensionString))
                continue;

            sb.AppendLine(extensionString);
            processedMembersCount++;
        }

        sb.AppendLine("}");

        if (processedMembersCount > 0)
        {
            context.AddSource($"{safeId}EventExtensions.g.cs", SourceText.From(sb.ToString(), Encoding.UTF8));
        }
    }

    // Rewritten for clarity: separates logic for EventHandler, EventHandler<T>, Action, and Action<T>.
    private static string GetEventExtension(string controlTypeName, EventFieldDeclarationSyntax @event)
    {
        // Get the event type as string (e.g., EventHandler, EventHandler<T>, Action, Action<T>)
        var eventTypeSyntax = @event.Declaration.Type;
        var eventTypeString = eventTypeSyntax.ToString();

        // Get the event name (e.g., Click)
        var eventName = @event.Declaration.Variables[0].ToString();
        var extensionName = $"On{eventName}";

        // Try to get the generic type arguments if present
        var genericName = eventTypeSyntax.DescendantNodesAndSelf().OfType<GenericNameSyntax>().FirstOrDefault();
        var typeArguments = genericName?.TypeArgumentList.Arguments.Select(arg => arg.ToString()).ToArray();

        // Determine the extension method signature and lambda body
        string parameterList;
        string lambdaParameters;
        string lambdaBody;
        bool isEventHandler = eventTypeString.StartsWith("EventHandler");
        bool isAction = eventTypeString.StartsWith("Action");

        if (isEventHandler && typeArguments is { Length: 1 })
        {
            // EventHandler<TEventArgs>
            parameterList = $"Action<{typeArguments[0]}> action";
            lambdaParameters = "_, args";
            lambdaBody = "action(args)";
        }
        else if (isEventHandler)
        {
            // EventHandler (no generic args)
            parameterList = "Action<EventArgs> action";
            lambdaParameters = "_, args";
            lambdaBody = "action(args)";
        }
        else if (isAction && typeArguments is { Length: 1 })
        {
            // Action<T>
            parameterList = $"Action<{typeArguments[0]}> action";
            lambdaParameters = "args";
            lambdaBody = "action(args)";
        }
        else if (isAction)
        {
            // Action (no args)
            parameterList = "Action action";
            lambdaParameters = "";
            lambdaBody = "action()";
        }
        else
        {
            // Fallback: treat as Action (no args)
            parameterList = "Action action";
            lambdaParameters = "";
            lambdaBody = "action()";
        }

        // Compose the extension method
        var extensionText =
            $"    public static {controlTypeName} {extensionName}"
            + $"(this {controlTypeName} control, {parameterList}) =>\n"
            + $"        control._setEvent(({eventTypeString})(({lambdaParameters}) => {lambdaBody}), h => control.{eventName} += h);";

        return extensionText;
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/AvaloniaPropertyExtensionsGenerator.cs">
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.CSharp;
using Microsoft.CodeAnalysis.CSharp.Syntax;
using Microsoft.CodeAnalysis.Text;
using System.Diagnostics;
using System.Text;
using static Avalonia.Markup.Declarative.SourceGenerator.MarkupTypeHelpers;

namespace Avalonia.Markup.Declarative.SourceGenerator;

[Generator]
public class AvaloniaPropertyExtensionsGenerator : IIncrementalGenerator
{

    public void Initialize(IncrementalGeneratorInitializationContext context)
    {
#if DEBUG
        if (!Debugger.IsAttached)
        {
            //Debugger.Launch();
        }
#endif
        Debug.WriteLine("Initialize AvaloniaPropertyExtensionsGenerator code generator");

        var classDeclarations = context.SyntaxProvider
            .CreateSyntaxProvider(
                predicate: static (s, _) => s is ClassDeclarationSyntax,
                transform: static (ctx, _) => GetSemanticTarget(ctx))
            .Where(static c => c is not null);

        context.RegisterSourceOutput(classDeclarations,
            static (spc, data) =>
            {
                if (data is { } value)
                {
                    GenerateSource(spc, value.Syntax, value.Model);
                }
            });
    }

    private static (ClassDeclarationSyntax Syntax, SemanticModel Model)? GetSemanticTarget(GeneratorSyntaxContext context)
    {
        var classDecl = (ClassDeclarationSyntax)context.Node;
        var symbol = context.SemanticModel.GetDeclaredSymbol(classDecl);
        
        // Check if this class or any of its base types implements IDeclarativeViewBase
        return symbol is INamedTypeSymbol typeSymbol &&
               ImplementsIDeclarativeViewBase(typeSymbol)
            ? (classDecl, context.SemanticModel)
            : null;
    }

    private static bool ImplementsIDeclarativeViewBase(INamedTypeSymbol typeSymbol)
    {
        // Check direct interfaces
        if (typeSymbol.AllInterfaces.Any(x => x.Name == "IDeclarativeViewBase"))
            return true;

        // Check base types (for inheritance)
        var current = typeSymbol.BaseType;
        while (current != null)
        {
            if (current.AllInterfaces.Any(x => x.Name == "IDeclarativeViewBase"))
                return true;
            current = current.BaseType;
        }

        return false;
    }

    private static void GenerateSource(SourceProductionContext context, ClassDeclarationSyntax type, SemanticModel semanticModel)
    {
        var root = type.SyntaxTree.GetRoot();
        var ns = root.DescendantNodes()
            .FirstOrDefault(x => x is BaseNamespaceDeclarationSyntax) as BaseNamespaceDeclarationSyntax;

        var typeNamespace = ns?.Name.ToString() ?? string.Empty;
        var sb = new StringBuilder();

        sb.AppendLine("#nullable enable");
        sb.AppendLine($"// Auto-generated code {DateTime.Now:g}");
        sb.AppendLine("using System;");
        sb.AppendLine("using Avalonia.Data;");
        sb.AppendLine("using Avalonia.Data.Converters;");
        sb.AppendLine("using System.Linq.Expressions;");

        if (root is CompilationUnitSyntax compilationUnit)
        {
            foreach (var usingDirective in compilationUnit.Usings)
            {
                sb.AppendLine(usingDirective.ToString());
            }
        }

        if (!string.IsNullOrWhiteSpace(typeNamespace))
            sb.AppendLine($"using {typeNamespace};");

        // Get the type symbol to build proper qualified names
        var typeSymbol = semanticModel.GetDeclaredSymbol(type) as INamedTypeSymbol;
        
        // Build the extension class name - handle nested classes properly
        var extensionClassName = BuildExtensionClassName(typeSymbol);
        
        // Fully qualified control type name including namespace, containing types and type parameters
        var controlTypeQualified = typeSymbol?.ToDisplayString(
            SymbolDisplayFormat.FullyQualifiedFormat
                .WithGenericsOptions(SymbolDisplayGenericsOptions.IncludeTypeParameters | SymbolDisplayGenericsOptions.IncludeVariance)
                .WithMiscellaneousOptions(SymbolDisplayMiscellaneousOptions.UseSpecialTypes | SymbolDisplayMiscellaneousOptions.ExpandNullable)
        ) ?? type.Identifier.ToString();

        // Build method generic parameters for all containing type parameters (outermost -> innermost)
        string allTypeParamsForMethods = BuildAllTypeParameters(type);

        sb.AppendLine("namespace Avalonia.Markup.Declarative;");
        sb.AppendLine($"public static partial class {extensionClassName}Extensions");
        sb.AppendLine("{");

        var members = type.Members;
        var processedFields = new List<string>();

        // PROCESS AVALONIA PROPERTIES
        foreach (var field in members.OfType<FieldDeclarationSyntax>())
        {
            if (field.Declaration.Type is GenericNameSyntax { Identifier.ValueText: "DirectProperty" or "StyledProperty" or "AttachedProperty" }
                && HasAvaloniaPropertyPublicSetter(field, members))
            {
                sb.AppendLine($"// avalonia properties\n");
                AppendIfNotNull(sb, GetPropertySetterExtension(controlTypeQualified, allTypeParamsForMethods, field));
                AppendIfNotNull(sb, GetExpressionBindingSetterExtension(controlTypeQualified, allTypeParamsForMethods, field));
                processedFields.Add(field.Declaration.Variables[0].Identifier.ValueText);
            }
        }

        // PROCESS COMMON PROPERTIES
        foreach (var property in members.OfType<PropertyDeclarationSyntax>())
        {
            var propertyName = property.Identifier.ToString();
            if (!processedFields.Contains(propertyName + "Property")
                && IsPublic(property)
                && HasPublicSetter(property)
                && IsCommonInstanceProperty(property, members))
            {
                sb.AppendLine($"// common properties\n");

                AppendIfNotNull(sb, GetCommonPropertySetterExtension(controlTypeQualified, property, semanticModel));
                // AppendIfNotNull(sb, GetCommonPropertyBindingSetterExtension(controlTypeQualified, property, semanticModel));
                AppendIfNotNull(sb, GetCommonPropertyExpressionBindingSetterExtension(controlTypeQualified, property, semanticModel));

                processedFields.Add(propertyName);
            }
        }

        sb.AppendLine("}");

        if (processedFields.Count > 0)
        {
            var fileName = RemoveIllegalFileNameCharacters(extensionClassName) + "Extensions.g.cs";
            context.AddSource(fileName, SourceText.From(sb.ToString(), Encoding.UTF8));
        }
    }

    private static string BuildExtensionClassName(INamedTypeSymbol? typeSymbol)
    {
        if (typeSymbol == null)
            return "Unknown";

        var parts = new List<string>();
        
        // Build from innermost to outermost
        var current = typeSymbol;
        while (current != null)
        {
            var name = current.Name;
            if (current.TypeParameters.Length > 0)
            {
                name += $"`{current.TypeParameters.Length}";
            }
            parts.Insert(0, name);
            
            current = current.ContainingType;
        }
        
        return CleanIdentifier(string.Join("_", parts)) ?? "Unknown";
    }

    private static string BuildAllTypeParameters(ClassDeclarationSyntax type)
    {
        var stack = new Stack<string>();
        SyntaxNode? current = type;
        while (current is ClassDeclarationSyntax cds)
        {
            if (cds.TypeParameterList is { Parameters.Count: > 0 })
            {
                // Push in reverse so that when we read Stack.ToArray() we keep original order per containing type
                foreach (var p in cds.TypeParameterList.Parameters.Reverse())
                {
                    stack.Push(p.Identifier.Text);
                }
            }
            current = current.Parent;
            if (current is BaseNamespaceDeclarationSyntax)
                break;
        }

        if (stack.Count == 0)
            return string.Empty;

        var arr = stack.ToArray(); // outermost -> innermost order
        return $"<{string.Join(", ", arr)}>";
    }

    public static string RemoveIllegalFileNameCharacters(string fileName)
    {
        if (string.IsNullOrEmpty(fileName))
            throw new ArgumentException("File name cannot be null or empty", nameof(fileName));

        // Remove invalid characters from the input
        string sanitizedFileName = new([.. fileName.Where(c => !Path.GetInvalidFileNameChars().Contains(c))]);

        return sanitizedFileName;
    }

    private static string? CleanIdentifier(string name, bool @namespace = false)
    {
        // trim off leading and trailing whitespace
        name = name.Trim();

        if (string.IsNullOrEmpty(name))
        {
            return null;
        }

        var sb = new StringBuilder();
        if (!SyntaxFacts.IsIdentifierStartCharacter(name[0]))
        {
            // the first characters
            sb.Append('_');
        }

        foreach (var ch in name)
        {
            if (SyntaxFacts.IsIdentifierPartCharacter(ch) || (@namespace && ch == '.'))
            {
                sb.Append(ch);
            }
        }

        var result = sb.ToString();

        if (SyntaxFacts.GetKeywordKind(result) != SyntaxKind.None)
        {
            result = '@' + result;
        }

        if (@namespace)
        {
            var newResult = string.Empty;
            foreach (var chunk in result.Split('.'))
            {
                if (!string.IsNullOrEmpty(newResult))
                {
                    newResult += '.';
                }

                if (SyntaxFacts.GetKeywordKind(chunk) != SyntaxKind.None)
                {
                    newResult += '@' + chunk;
                }
                else
                {
                    newResult += chunk;
                }
            }

            return newResult;
        }

        return result;
    }

    private static void AppendIfNotNull(StringBuilder sb, string value)
    {
        if (string.IsNullOrWhiteSpace(value)) return;
        sb.AppendLine(value);
    }

    public static string GetPropertySetterExtension(string controlTypeName, string genericParamsAll, FieldDeclarationSyntax field)
    {
        var extensionName = field.Declaration.Variables[0].Identifier.ToString().Replace("Property", "");

        var genericName = field.Declaration.Type as GenericNameSyntax;
        if (genericName == null)
            return string.Empty;

        var valueTypeSource = genericName.TypeArgumentList.Arguments.Last();

        var valueName = valueTypeSource is NullableTypeSyntax nullableTypeSyntax
            ? nullableTypeSyntax.ElementType.ToString()
            : valueTypeSource.ToString();

        // Get Class constraints
        var classConstraint = "";
        if (field.Parent is ClassDeclarationSyntax classDecleration)
        {
            foreach (TypeParameterConstraintClauseSyntax constraintClause in classDecleration.ConstraintClauses)
            {
                if (constraintClause.Name.ToString() == valueName)
                {
                    classConstraint = " " + constraintClause.ToString();
                }
            }
        }

        var extensionText =
            $"public static {controlTypeName} {extensionName}{genericParamsAll}" +
            $"(this {controlTypeName} control, {valueTypeSource} value){classConstraint}{NewLine}" +
            $"=> control._set(() => control.{extensionName} = value!);";

        return extensionText;
    }

    private static string GetCommonPropertySetterExtension(string controlTypeName, PropertyDeclarationSyntax property, SemanticModel semanticModel)
    {
        var extensionName = property.Identifier.ToString();

        var valueTypeSource = GetPropertyTypeName(property, semanticModel);

        var extensionText =
            $"public static {controlTypeName} {extensionName}" +
            $"(this {controlTypeName} control, {valueTypeSource} value)" +
            $"=>{NewLine} control._set(() => control.{extensionName} = value);";

        return extensionText;
    }

    // private static string GetCommonPropertyBindingSetterExtension(string controlTypeName, PropertyDeclarationSyntax property, SemanticModel semanticModel)
    // {
    //     var extensionName = property.Identifier.ToString();
    //     var valueTypeSource = GetPropertyTypeName(property, semanticModel);

    //     var extensionText =
    //         $"#pragma warning disable CS8601{NewLine}" +
    //         $"[Obsolete] public static {controlTypeName} {extensionName}" +
    //         $"(this {controlTypeName} control, IBinding binding)" +
    //         $"=>{NewLine} control._setCommonBindingEx(({valueTypeSource}? v) => control.{extensionName} = v ?? default({valueTypeSource}), binding);{NewLine}" +
    //         $"#pragma warning restore CS8601";

    //     return extensionText;
    // }
    public static string GetExpressionBindingSetterExtension(string controlTypeName, string genericParamsAll, FieldDeclarationSyntax field)
    {
        var extensionName = field.Declaration.Variables[0].Identifier.ToString().Replace("Property", "");

        var genericName = field.Declaration.Type as GenericNameSyntax;
        if (genericName == null)
            return string.Empty;

        var valueTypeSource = genericName.TypeArgumentList.Arguments.Last();

        var valueName = valueTypeSource is NullableTypeSyntax nullableTypeSyntax
            ? nullableTypeSyntax.ElementType.ToString()
            : valueTypeSource.ToString();

        // Get Class constraints
        var classConstraint = "";
        if (field.Parent is ClassDeclarationSyntax classDecleration)
        {
            foreach (TypeParameterConstraintClauseSyntax constraintClause in classDecleration.ConstraintClauses)
            {
                if (constraintClause.Name.ToString() == valueName)
                {
                    classConstraint = " " + constraintClause.ToString();
                }
            }
        }

        // Check if TViewModel already exists as a type parameter (e.g. ReactiveViewBase<TViewModel>)
        var existingParams = string.IsNullOrEmpty(genericParamsAll) 
            ? Array.Empty<string>() 
            : genericParamsAll.Trim('<', '>').Split(',').Select(p => p.Trim()).ToArray();
        var hasTViewModel = existingParams.Contains("TViewModel");

        var genericParamsCombined = hasTViewModel
            ? genericParamsAll // TViewModel already present, no need to add
            : string.IsNullOrEmpty(genericParamsAll)
                ? "<TViewModel>"
                : genericParamsAll.Insert(1, "TViewModel, "); // e.g. "<T>" -> "<TViewModel, T>"

        var extensionText =
            $"public static {controlTypeName} {extensionName}{genericParamsCombined}(this {controlTypeName} control, TViewModel source, Expression<Func<TViewModel, {valueTypeSource}>> getter, BindingMode? mode = null, IValueConverter? converter = null){classConstraint}{NewLine}" +
            $"   => control._setCompiledBinding({controlTypeName}.{extensionName}Property, source, getter, mode, converter);";

        return extensionText;
    }

    private static string GetCommonPropertyExpressionBindingSetterExtension(string controlTypeName, PropertyDeclarationSyntax property, SemanticModel semanticModel)
    {
        var extensionName = property.Identifier.ToString();
        var valueTypeSource = GetPropertyTypeName(property, semanticModel);

        var extensionText =
            $"//Generated by GetCommonPropertyExpressionBindingSetterExtension{NewLine}" +
            $"public static {controlTypeName} {extensionName}<TViewModel>(this {controlTypeName} control, TViewModel source, Expression<Func<TViewModel, {valueTypeSource}>> getter, BindingMode? mode = null, IValueConverter? converter = null){NewLine}" +
            $"   => control._setCompiledBinding(null!, source, getter, mode, converter);";


        return extensionText;
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/AttachedPropertyExtensionInfo.cs">
#nullable enable

using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

internal sealed class AttachedPropertyExtensionInfo : PropertyExtensionInfo
{
    public string? AttachedPropertyHostTypeName { get; }

    public AttachedPropertyExtensionInfo(IFieldSymbol field)
        : base(field)
    {
        ExtensionName = $"{field.ContainingType.Name.Split('`')[0]}_{ExtensionName}";

        if (field.ContainingType.GetMembers($"Set{MemberName}").FirstOrDefault() is IMethodSymbol setter &&
            setter.Parameters.Length > 0)
        {
            AttachedPropertyHostTypeName = setter.Parameters[0].Type.GetFullTypeName();
            ReturnType = "T";
            GenericConstraint = $"where T : {AttachedPropertyHostTypeName}";
            GenericArg = "<T>";
        }
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/EventExtensionInfo.cs">
#nullable enable

using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

internal sealed class EventExtensionInfo : IMemberExtensionInfo
{
    public string MemberName { get; }
    public string ControlTypeName { get; }
    public string EventHandler { get; }
    public string EventName { get; }
    public List<string> EventParameterTypes { get; } = [];
    public bool ReturnsVoid { get; private set; } = true;
    public string ReturnType { get; }
    public string GenericConstraint { get; } = "";
    public string GenericArg { get; } = "";
    public bool IsRoutedEvent { get; }
    public bool IsObsolete { get; }

    public bool HasStandardSignature =>
        EventParameterTypes.Count == 2 &&
        EventParameterTypes[0] == "global::System.Object?" &&
        EventParameterTypes[1].Contains("EventArgs", StringComparison.Ordinal);

    public EventExtensionInfo(IEventSymbol eventInfo)
    {
        ControlTypeName = eventInfo.ContainingType.GetFullTypeName();
        EventName = eventInfo.Name;
        MemberName = EventName;
        EventHandler = eventInfo.Type.GetFullTypeName();
        IsObsolete = eventInfo.IsObsolete();
        IsRoutedEvent = IsRoutedEventSymbol(eventInfo);

        if (!eventInfo.ContainingType.IsSealed)
        {
            ReturnType = "T";
            GenericConstraint = $"where T : {ControlTypeName}";
            GenericArg = "<T>";
        }
        else
        {
            ReturnType = ControlTypeName;
        }

        if (eventInfo.Type is INamedTypeSymbol delegateType && delegateType.DelegateInvokeMethod is { } invokeMethod)
        {
            ReturnsVoid = invokeMethod.ReturnsVoid;

            foreach (var parameter in invokeMethod.Parameters)
            {
                EventParameterTypes.Add(parameter.Type.GetFullTypeName());
            }
        }
    }

    private static bool IsRoutedEventSymbol(IEventSymbol eventInfo)
    {
        if (eventInfo.Type is not INamedTypeSymbol delegateType || delegateType.DelegateInvokeMethod is not { } invokeMethod)
        {
            return false;
        }

        var hasRoutedArgs = invokeMethod.Parameters.Any(static x => x.Type.IsOrInheritsFrom("Avalonia.Interactivity.RoutedEventArgs"));
        if (!hasRoutedArgs)
        {
            return false;
        }

        return eventInfo.ContainingType.GetMembers($"{eventInfo.Name}Event")
            .OfType<IFieldSymbol>()
            .Any(static x => x.IsStatic && x.DeclaredAccessibility == Accessibility.Public);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/IMemberExtensionInfo.cs">
namespace Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

internal interface IMemberExtensionInfo
{
    string MemberName { get; }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExtensionInfos/PropertyExtensionInfo.cs">
#nullable enable

using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

internal class PropertyExtensionInfo : IMemberExtensionInfo
{
    public IFieldSymbol FieldSymbol { get; }
    public INamedTypeSymbol ControlType { get; }
    public string ControlTypeName { get; }
    public string ExtensionName { get; protected set; }
    public string MemberName { get; }
    public ITypeSymbol ValueType { get; }
    public string ValueTypeSource { get; }
    public bool IsGeneric { get; }
    public string ReturnType { get; protected set; }
    public string GenericConstraint { get; protected set; } = "";
    public string StyleGenericConstraint { get; protected set; } = "";
    public string GenericArg { get; protected set; } = "";

    public PropertyExtensionInfo(IFieldSymbol field)
    {
        FieldSymbol = field;
        ControlType = field.ContainingType ?? throw new NullReferenceException("Control type can not be NULL");
        ExtensionName = field.GetMemberName();
        MemberName = field.GetMemberName();
        ValueType = field.Type.GetLastGenericArgument();
        ControlTypeName = ControlType.GetFullTypeName();
        ValueTypeSource = ValueType.GetFullTypeName();

        IsGeneric = !ControlType.IsSealed;
        ReturnType = ControlTypeName;

        if (IsGeneric)
        {
            ReturnType = "T";
            GenericConstraint = $"where T : {ControlTypeName}";
            StyleGenericConstraint = $"where T : {ControlType.GetStyleConstraintTypeName()}";
            GenericArg = "<T>";
        }
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExternalAssemblyExtensionsGenerator.cs">
using System.Text;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.Text;

namespace Avalonia.Markup.Declarative.SourceGenerator;

[Generator]
public sealed class ExternalAssemblyExtensionsGenerator : IIncrementalGenerator
{
    public void Initialize(IncrementalGeneratorInitializationContext context)
    {
        var selectedAssemblies = context.CompilationProvider
            .Select(static (compilation, _) => SymbolUtilities.GetTargetAssemblies(compilation));

        context.RegisterSourceOutput(selectedAssemblies, static (spc, assemblies) =>
        {
            var generatorHost = new ExternalGeneratorHost();

            foreach (var assembly in assemblies)
            {
                foreach (var publicClass in SymbolUtilities.GetPublicClasses(assembly.GlobalNamespace))
                {
                    if (!publicClass.IsOrInheritsFrom("Avalonia.AvaloniaObject"))
                    {
                        continue;
                    }

                    try
                    {
                        var code = generatorHost.GenerateExtensions(publicClass);
                        if (string.IsNullOrWhiteSpace(code))
                        {
                            continue;
                        }

                        var hintName = $"External_{SymbolUtilities.RemoveIllegalFileNameCharacters(publicClass.ToDisplayString())}.g.cs";
                        spc.AddSource(hintName, SourceText.From(code!, Encoding.UTF8));
                    }
                    catch (Exception ex)
                    {
                        _ = ex;
                    }
                }
            }
        });
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/ExternalGeneratorHost.cs">
using System.Collections.Immutable;
using System.Text;
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;
using Avalonia.Markup.Declarative.SourceGenerator.Generators;
using Avalonia.Markup.Declarative.SourceGenerator.Generators.AttachedPropertySetterGenerator;
using Avalonia.Markup.Declarative.SourceGenerator.Generators.EventGenerators;
using Avalonia.Markup.Declarative.SourceGenerator.Generators.SetterGenerators;
using Avalonia.Markup.Declarative.SourceGenerator.Generators.StyleSetterGenerators;
using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator;

internal sealed class ExternalGeneratorHost
{
    private readonly List<ExtensionGroupGenerator> _groupGenerators =
    [
        new("Properties",
            static t => t.GetMembers()
                .OfType<IFieldSymbol>()
                .Where(static x => x.IsAvaloniaPropertyField() && !x.HasUnsupportedExternalValueType())
                .Select(static x => new PropertyExtensionInfo(x)),

            new ValueSetterGenerator(),
            new BindFromExpressionSetterGenerator(),
            new AvaloniaPropertyBindSetterGenerator(),
            new ValueOverloadsSetterGenerator()
        ),

        new("Attached Properties",
            static t => t.GetMembers()
                .OfType<IFieldSymbol>()
                .Where(static x => x.IsAttachedPropertyField())
                .Select(static x => new AttachedPropertyExtensionInfo(x))
                .Where(static x => !string.IsNullOrWhiteSpace(x.AttachedPropertyHostTypeName)),

            new AttachedPropertyValueSetterGenerator(),
            new AttachedPropertyBindFromExpressionSetterGenerator()
        ),

        new("Events",
            static t => t.GetMembers()
                .OfType<IEventSymbol>()
                .Where(x => x.DeclaredAccessibility == Accessibility.Public && SymbolEqualityComparer.Default.Equals(x.ContainingType, t))
                .Select(static x => new EventExtensionInfo(x)),

            new ActionToEventGenerator()
        ),

        new("Styles",
            static t => !t.IsStyledElement() ? [] : GetStyledFields(t)
                .Where(static x => !x.HasUnsupportedExternalValueType())
                .Select(static x => new PropertyExtensionInfo(x)),

            new ValueStyleSetterGenerator(),
            new BindFromExpressionStyleSetterGenerator(),
            new ValueOverloadsStyleSetterGenerator()
        )
    ];

    private static IEnumerable<IFieldSymbol> GetStyledFields(INamedTypeSymbol type)
    {
        var fields = type.GetMembers().OfType<IFieldSymbol>();

        if (type.GetFullTypeName() == "global::Avalonia.StyledElement")
        {
            fields = fields.Concat(type.GetAllMembers()
                .OfType<IFieldSymbol>()
                .Where(x => x.ContainingType is not null &&
                            !SymbolEqualityComparer.Default.Equals(x.ContainingType, type) &&
                            !x.ContainingType.IsOrInheritsFrom("Avalonia.StyledElement")));
        }

        return fields.Where(static x => x.IsAcceptableStyledField());
    }

    public string? GenerateExtensions(INamedTypeSymbol controlType)
    {
        var extensionGroups = _groupGenerators
            .Select(x =>
            {
                var extensions = x.Generate(controlType, out var generatedCount);
                return (x.GroupName, extensions, generatedCount);
            })
            .ToImmutableArray();

        if (extensionGroups.All(static x => x.generatedCount == 0))
        {
            return null;
        }

        var sb = new StringBuilder();
        sb.AppendLine("#nullable enable");
        sb.AppendLine("using Avalonia;");
        sb.AppendLine("using Avalonia.Data;");
        sb.AppendLine("using Avalonia.Data.Converters;");
        sb.AppendLine("using System;");
        sb.AppendLine("using System.Linq.Expressions;");
        sb.AppendLine("using System.Runtime.CompilerServices;");
        sb.AppendLine();
        sb.AppendLine("namespace Avalonia.Markup.Declarative;");
        sb.AppendLine("[global::System.CodeDom.Compiler.GeneratedCode(\"Avalonia.Markup.Declarative.SourceGenerator\", \"1.0.0.0\")]");
        sb.AppendLine("[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]");
        sb.AppendLine($"public static partial class {SymbolUtilities.BuildExtensionClassName(controlType)}");
        sb.AppendLine("{");

        foreach (var group in extensionGroups.Where(static x => x.extensions != null))
        {
            sb.AppendLine($"//================= {group.GroupName} ======================//");
            sb.Append(group.extensions);
        }

        sb.AppendLine("}");
        return sb.ToString();
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/AttachedPropertySetterGenerator/AttachedPropertyBindFromExpressionSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.AttachedPropertySetterGenerator;

internal sealed class AttachedPropertyBindFromExpressionSetterGenerator : ExtensionGeneratorBase<AttachedPropertyExtensionInfo>
{
    protected override string? GetExtension(AttachedPropertyExtensionInfo info)
    {
        if (string.IsNullOrWhiteSpace(info.AttachedPropertyHostTypeName))
        {
            return null;
        }

        return $"public static T {info.ExtensionName}<T, TViewModel>(this T control, TViewModel source, Expression<Func<TViewModel, {info.ValueTypeSource}>> getter, BindingMode? mode = null, IValueConverter? converter = null) where T : {info.AttachedPropertyHostTypeName} {SymbolUtilities.NewLine}" +
               $"   => control._setCompiledBinding({info.ControlTypeName}.{info.FieldSymbol.Name}!, source, getter, mode, converter);";
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/AttachedPropertySetterGenerator/AttachedPropertyValueSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.AttachedPropertySetterGenerator;

internal sealed class AttachedPropertyValueSetterGenerator : ExtensionGeneratorBase<AttachedPropertyExtensionInfo>
{
    protected override string? GetExtension(AttachedPropertyExtensionInfo info)
    {
        if (string.IsNullOrWhiteSpace(info.AttachedPropertyHostTypeName))
        {
            return null;
        }

        return $"public static T {info.ExtensionName}<T>(this T control, {info.ValueTypeSource} value) where T : {info.AttachedPropertyHostTypeName} {SymbolUtilities.NewLine}" +
               $"   => control._set(() => {info.ControlTypeName}.Set{info.MemberName}(control, value!));";
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/EventGenerators/ActionToEventGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.EventGenerators;

internal sealed class ActionToEventGenerator : ExtensionGeneratorBase<EventExtensionInfo>
{
    protected override string GetExtension(EventExtensionInfo @event)
    {
        if (!@event.ReturnsVoid)
        {
            return
                (@event.IsObsolete ? "[Obsolete]" + SymbolUtilities.NewLine : string.Empty) +
                $"public static {@event.ReturnType} On{@event.EventName}{@event.GenericArg}(this {@event.ReturnType} control, {@event.EventHandler} handler) {@event.GenericConstraint} " +
                $"=> {SymbolUtilities.NewLine} control._setEvent(handler, h => control.{@event.EventName} += h);";
        }

        var eventParameterTypes = @event.EventParameterTypes;
        string argsString;
        string lambdaParameters;
        string actionCall;

        if (eventParameterTypes.Count == 0)
        {
            argsString = "Action action";
            lambdaParameters = string.Empty;
            actionCall = "action()";
        }
        else if (@event.HasStandardSignature)
        {
            argsString = $"Action<{string.Join(", ", eventParameterTypes.Skip(1))}> action";
            lambdaParameters = string.Join(", ", eventParameterTypes.Select((_, index) => $"arg{index}"));
            actionCall = "action(arg1)";
        }
        else if (eventParameterTypes.Count == 1)
        {
            argsString = $"Action<{eventParameterTypes[0]}> action";
            lambdaParameters = "arg0";
            actionCall = "action(arg0)";
        }
        else
        {
            argsString = $"Action<{string.Join(", ", eventParameterTypes)}> action";
            lambdaParameters = string.Join(", ", eventParameterTypes.Select((_, index) => $"arg{index}"));
            actionCall = $"action({lambdaParameters})";
        }

        var extensionBody =
            $" => {SymbolUtilities.NewLine} control._setEvent(({@event.EventHandler}) (({lambdaParameters}) => {actionCall}), h => control.{@event.EventName} += h);";

        if (@event.IsRoutedEvent)
        {
            argsString += ", Avalonia.Interactivity.RoutingStrategies? routes = null";
            extensionBody =
                SymbolUtilities.NewLine + "{" + SymbolUtilities.NewLine +
                $"  control.AddHandler({@event.ControlTypeName}.{@event.EventName}Event, (_, args) => action(args), routes ?? {@event.ControlTypeName}.{@event.EventName}Event.RoutingStrategies);" + SymbolUtilities.NewLine +
                "  return control;" + SymbolUtilities.NewLine +
                "}" + SymbolUtilities.NewLine;
        }

        return
            (@event.IsObsolete ? "[Obsolete]" + SymbolUtilities.NewLine : string.Empty) +
            $"public static {@event.ReturnType} On{@event.EventName}{@event.GenericArg}(this {@event.ReturnType} control, {argsString}) {@event.GenericConstraint} " +
            extensionBody;
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/ExtensionGeneratorBase.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators;

internal abstract class ExtensionGeneratorBase<TMemberExtensionInfo> : ExtensionGeneratorBase
{
    protected abstract string? GetExtension(TMemberExtensionInfo info);

    public override string? GetExtension(IMemberExtensionInfo info) => GetExtension((TMemberExtensionInfo)info);
}

internal abstract class ExtensionGeneratorBase
{
    public abstract string? GetExtension(IMemberExtensionInfo info);
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/ExtensionGroupGenerator.cs">
using System.Text;
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;
using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators;

internal sealed class ExtensionGroupGenerator(
    string groupName,
    Func<INamedTypeSymbol, IEnumerable<IMemberExtensionInfo>> membersQuery,
    params ExtensionGeneratorBase[] generators)
{
    public string GroupName { get; } = groupName;

    public string? Generate(INamedTypeSymbol avaloniaControlType, out int generationsCount)
    {
        generationsCount = 0;

        var sb = new StringBuilder();
        foreach (var extensionInfo in membersQuery(avaloniaControlType))
        {
            sb.AppendLine($" // {extensionInfo.MemberName}");
            foreach (var generator in generators)
            {
                var extensionCode = generator.GetExtension(extensionInfo);
                if (string.IsNullOrWhiteSpace(extensionCode))
                {
                    continue;
                }

                sb.AppendLine();
                sb.AppendLine($"/*{generator.GetType().Name}*/");
                sb.AppendLine(extensionCode);
                generationsCount++;
            }

            sb.AppendLine();
            sb.AppendLine();
        }

        return generationsCount == 0 ? null : sb.ToString();
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/AvaloniaPropertyBindSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.SetterGenerators;

internal sealed class AvaloniaPropertyBindSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info) =>
        $"public static {info.ReturnType} {info.ExtensionName}{info.GenericArg}(this {info.ReturnType} control, AvaloniaProperty avaloniaProperty, AvaloniaObject source, BindingMode? bindingMode = null, IValueConverter? converter = null) {info.GenericConstraint} {SymbolUtilities.NewLine}" +
        $"   => control._setPropertyBinding({info.ControlTypeName}.{info.FieldSymbol.Name}, avaloniaProperty, source, bindingMode, converter);";
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/BindFromExpressionSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.SetterGenerators;

internal sealed class BindFromExpressionSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info)
    {
        var genericParams = info.IsGeneric ? "<T, TViewModel>" : "<TViewModel>";
        return $"public static {info.ReturnType} {info.ExtensionName}{genericParams}(this {info.ReturnType} control, TViewModel source, Expression<Func<TViewModel, {info.ValueTypeSource}>> getter, BindingMode? mode = null, IValueConverter? converter = null) {info.GenericConstraint} {SymbolUtilities.NewLine}" +
               $"   => control._setCompiledBinding({info.ControlTypeName}.{info.FieldSymbol.Name}!, source, getter, mode, converter);";
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/ValueOverloadsSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;
using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.SetterGenerators;

internal sealed class ValueOverloadsSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info)
    {
        var extensionText = string.Empty;

        if (!info.ValueType.ContainingNamespace.ToDisplayString().StartsWith("System", StringComparison.Ordinal) &&
            info.ValueType.IsValueType)
        {
            foreach (var constructor in info.ValueType.GetMembers().OfType<IMethodSymbol>().Where(static m => m.MethodKind == MethodKind.Constructor))
            {
                var ps = constructor.Parameters;
                if (ps.Length == 0)
                {
                    continue;
                }

                var argDefs = string.Join(", ", ps.Select(static x => $"{x.Type.GetFullTypeName()} {x.Name}"));
                var argVals = string.Join(", ", ps.Select(static x => x.Name));

                extensionText += $"{SymbolUtilities.NewLine}public static {info.ReturnType} {info.ExtensionName}{info.GenericArg}(this {info.ReturnType} control, {argDefs}) {info.GenericConstraint} {SymbolUtilities.NewLine}" +
                                 $"   => control._set(() => control.{info.MemberName} = new {info.ValueTypeSource}({argVals}));";
            }
        }

        return extensionText;
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/SetterGenerators/ValueSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.SetterGenerators;

internal sealed class ValueSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info) =>
        $"public static {info.ReturnType} {info.ExtensionName}{info.GenericArg}(this {info.ReturnType} control, {info.ValueTypeSource} value) {info.GenericConstraint} {SymbolUtilities.NewLine}" +
        $"=> control._set(() => control.{info.MemberName} = value!);";
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/BindFromExpressionStyleSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.StyleSetterGenerators;

internal sealed class BindFromExpressionStyleSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string? GetExtension(PropertyExtensionInfo info)
    {
        if (info.ValueTypeSource is "global::Avalonia.Data.IBinding" or "global::Avalonia.Data.IBinding?")
        {
            return null;
        }

        var genericParams = info.IsGeneric ? "<T, TViewModel>" : "<TViewModel>";
        return $"public static Style<{info.ReturnType}> {info.ExtensionName}{genericParams}(this Style<{info.ReturnType}> style, TViewModel source, Expression<Func<TViewModel, {info.ValueTypeSource}>> getter, BindingMode? bindingMode = null, IValueConverter? converter = null) {info.StyleGenericConstraint} {SymbolUtilities.NewLine}" +
               $"   => style._addSetterCompiledBinding({info.ControlTypeName}.{info.MemberName}Property!, source, getter, bindingMode, converter);";
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/ValueOverloadsStyleSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;
using Microsoft.CodeAnalysis;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.StyleSetterGenerators;

internal sealed class ValueOverloadsStyleSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info)
    {
        var extensionText = string.Empty;

        if (!info.ValueType.ContainingNamespace.ToDisplayString().StartsWith("System", StringComparison.Ordinal) &&
            info.ValueType.IsValueType)
        {
            foreach (var constructor in info.ValueType.GetMembers().OfType<IMethodSymbol>().Where(static m => m.MethodKind == MethodKind.Constructor))
            {
                var ps = constructor.Parameters;
                if (ps.Length == 0)
                {
                    continue;
                }

                var argDefs = string.Join(", ", ps.Select(static x => $"{x.Type.GetFullTypeName()} {x.Name}"));
                var argVals = string.Join(", ", ps.Select(static x => x.Name));

                extensionText += $"public static Style<{info.ReturnType}> {info.ExtensionName}{info.GenericArg}(this Style<{info.ReturnType}> style, {argDefs}) {info.StyleGenericConstraint} {SymbolUtilities.NewLine}" +
                                 $"   => style._addSetter({info.ControlTypeName}.{info.MemberName}Property, new {info.ValueTypeSource}({argVals}));";
            }
        }

        return extensionText;
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/Generators/StyleSetterGenerators/ValueStyleSetterGenerator.cs">
using Avalonia.Markup.Declarative.SourceGenerator.ExtensionInfos;

namespace Avalonia.Markup.Declarative.SourceGenerator.Generators.StyleSetterGenerators;

internal sealed class ValueStyleSetterGenerator : ExtensionGeneratorBase<PropertyExtensionInfo>
{
    protected override string GetExtension(PropertyExtensionInfo info) =>
    $"public static Style<{info.ReturnType}> {info.ExtensionName}{info.GenericArg}(this Style<{info.ReturnType}> style, {info.ValueTypeSource} value) {info.StyleGenericConstraint} {SymbolUtilities.NewLine}" +
        $"=> style._addSetter({info.ControlTypeName}.{info.MemberName}Property!, value!);";
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/MarkupTypeHelpers.cs">
#nullable enable

using System.Collections.Generic;
using System.Collections.Immutable;
using System.Linq;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.CSharp;
using Microsoft.CodeAnalysis.CSharp.Syntax;

namespace Avalonia.Markup.Declarative.SourceGenerator;

internal static class MarkupTypeHelpers
{
    internal const string NewLine = "\n";

    internal static bool IsAvaloniaMarkupView(INamedTypeSymbol typeSymbol)
    {
        return typeSymbol.AllInterfaces
               .Any(x => x.Name == "IDeclarativeViewBase");
    }

    internal static ImmutableArray<ClassDeclarationSyntax> FindAvaloniaMarkupViews(Compilation compilation)
    {
        IEnumerable<SyntaxNode> allNodes = compilation.SyntaxTrees.SelectMany(s => s.GetRoot().DescendantNodes());
        IEnumerable<ClassDeclarationSyntax> allClasses = allNodes
            .Where(d => d.IsKind(SyntaxKind.ClassDeclaration))
            .OfType<ClassDeclarationSyntax>();

        return [..allClasses.Where(type => IsGenerateExtensionsView(compilation, type))];
    }

    private static bool IsGenerateExtensionsView(Compilation compilation, ClassDeclarationSyntax component)
    {
        var sModel = compilation.GetSemanticModel(component.SyntaxTree);
        var classSymbol = ModelExtensions.GetDeclaredSymbol(sModel, component);

        return classSymbol is INamedTypeSymbol cs && cs.AllInterfaces.Any(x => x.Name == "IDeclarativeViewBase");
    }

    /// <summary>
    /// Determines if property also has Avalonia property backend or not
    /// </summary>
    /// <param name="property">target property</param>
    /// <param name="members"></param>
    /// <returns></returns>
    internal static bool IsCommonInstanceProperty(PropertyDeclarationSyntax property, SyntaxList<MemberDeclarationSyntax> members)
    {
        if(property.Modifiers.Any(x=>x.Text == "static"))
            return false;

        var avaloniaPropertyName = property.Identifier + "Property";
        return members.OfType<FieldDeclarationSyntax>().All(field => field.Declaration.Variables[0].Identifier.ToString() != avaloniaPropertyName);
    }

    internal static bool HasAvaloniaPropertyPublicSetter(FieldDeclarationSyntax field, SyntaxList<MemberDeclarationSyntax> members)
    {
        var backingPropertyName = field.Declaration.Variables[0].Identifier.ToString().Replace("Property", "");

        var property = members
            .OfType<PropertyDeclarationSyntax>()
            .FirstOrDefault(x => x.Identifier.ValueText == backingPropertyName);

        return HasPublicSetter(property);
    }

    internal static bool HasPublicSetter(PropertyDeclarationSyntax? property)
    {
        if (property != null)
        {
            var setter = property.AccessorList?.Accessors.FirstOrDefault(x => x.IsKind(SyntaxKind.SetAccessorDeclaration));
            if (setter?.Modifiers.Any() == false)
                return true;
        }

        return false;
    }

    internal static bool IsPublic(PropertyDeclarationSyntax? property)
    {
        return property != null && property.Modifiers.Any(x => x.ValueText == "public");
    }

    internal static string GetPropertyTypeName(PropertyDeclarationSyntax property, SemanticModel semanticModel)
    {
        var typeInfo = semanticModel.GetTypeInfo(property.Type);
        return typeInfo.Type?.ToDisplayString() ?? property.Type.ToString();
    }

    internal static string GetNullableLambdaParameterTypeName(PropertyDeclarationSyntax property, SemanticModel semanticModel)
    {
        var typeInfo = semanticModel.GetTypeInfo(property.Type);
        var typeSymbol = typeInfo.Type;

        if (typeSymbol == null)
            return property.Type + "?";

        var nullableTypeSymbol = typeSymbol.WithNullableAnnotation(NullableAnnotation.Annotated);

        return nullableTypeSymbol.ToDisplayString(SymbolDisplayFormat.FullyQualifiedFormat);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.SourceGenerator/SymbolUtilities.cs">
using System.Collections.Immutable;
using System.Text;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.CSharp;

namespace Avalonia.Markup.Declarative.SourceGenerator;

internal static class SymbolUtilities
{
    internal const string NewLine = "\n";

    private static readonly HashSet<string> AutoFrameworkAssemblies =
    [
        "Avalonia.Base",
        "Avalonia.Controls",
        "Avalonia.Markup",
        "Avalonia.Themes.Fluent",
    ];

    private static readonly SymbolDisplayFormat FullyQualifiedGlobalFormat =
        new(
            globalNamespaceStyle: SymbolDisplayGlobalNamespaceStyle.Included,
            typeQualificationStyle: SymbolDisplayTypeQualificationStyle.NameAndContainingTypesAndNamespaces,
            genericsOptions:
                SymbolDisplayGenericsOptions.IncludeTypeParameters |
                SymbolDisplayGenericsOptions.IncludeVariance,
            miscellaneousOptions:
                SymbolDisplayMiscellaneousOptions.EscapeKeywordIdentifiers |
                SymbolDisplayMiscellaneousOptions.IncludeNullableReferenceTypeModifier
        );

    internal static ImmutableArray<IAssemblySymbol> GetTargetAssemblies(Compilation compilation)
    {
        var result = new HashSet<IAssemblySymbol>(SymbolEqualityComparer.Default);

        foreach (var reference in compilation.References)
        {
            if (compilation.GetAssemblyOrModuleSymbol(reference) is IAssemblySymbol assembly &&
                AutoFrameworkAssemblies.Contains(assembly.Name))
            {
                result.Add(assembly);
            }
        }

        foreach (var attribute in compilation.Assembly.GetAttributes())
        {
            if (attribute.AttributeClass?.ToDisplayString() != "Avalonia.Markup.Declarative.GenerateMarkupExtensionsForAssemblyAttribute")
            {
                continue;
            }

            if (attribute.ConstructorArguments.Length == 0)
            {
                continue;
            }

            if (attribute.ConstructorArguments[0].Value is INamedTypeSymbol anchorType)
            {
                result.Add(anchorType.ContainingAssembly);
            }
        }

        return [.. result.OrderBy(static x => x.Name)];
    }

    internal static IEnumerable<INamedTypeSymbol> GetPublicClasses(INamespaceSymbol sym)
    {
        foreach (var typeMember in sym.GetTypeMembers())
        {
            if (typeMember.DeclaredAccessibility == Accessibility.Public &&
                typeMember.TypeKind == TypeKind.Class &&
                !typeMember.IsGenericType)
            {
                yield return typeMember;
            }
        }

        foreach (var namespaceMember in sym.GetNamespaceMembers())
        {
            foreach (var publicClass in GetPublicClasses(namespaceMember))
            {
                yield return publicClass;
            }
        }
    }

    internal static IEnumerable<ISymbol> GetAllMembers(this INamedTypeSymbol type)
    {
        var seen = new HashSet<string>();
        var stack = new Stack<INamedTypeSymbol>();
        stack.Push(type);

        while (stack.Count > 0)
        {
            var current = stack.Pop();
            foreach (var member in current.GetMembers())
            {
                if (seen.Add(member.ToDisplayString()))
                {
                    yield return member;
                }
            }

            if (current.BaseType != null)
            {
                stack.Push(current.BaseType);
            }

            foreach (var iface in current.Interfaces)
            {
                stack.Push(iface);
            }
        }
    }

    internal static bool IsOrInheritsFrom(this ITypeSymbol? symbol, string metadataName)
    {
        while (symbol != null)
        {
            if (GetMetadataName(symbol) == metadataName)
            {
                return true;
            }

            symbol = symbol.BaseType;
        }

        return false;
    }

    internal static bool IsStyledElement(this INamedTypeSymbol controlType) =>
        controlType.IsOrInheritsFrom("Avalonia.StyledElement");

    internal static string GetStyleConstraintTypeName(this ITypeSymbol controlType) =>
        controlType.IsOrInheritsFrom("Avalonia.StyledElement")
            ? controlType.GetFullTypeName()
            : "global::Avalonia.StyledElement";

    internal static bool HasPublicSetter(this IPropertySymbol? property) =>
        property?.SetMethod != null && property.SetMethod.DeclaredAccessibility == Accessibility.Public;

    internal static bool IsAvaloniaPropertyField(this IFieldSymbol field)
    {
        if (field.IsObsolete())
        {
            return false;
        }

        if (field.Type.Name.StartsWith("DirectProperty") ||
            field.Type.Name.StartsWith("StyledProperty") ||
            field.Type.Name.StartsWith("AttachedProperty") ||
            field.Type.Name.StartsWith("AvaloniaProperty"))
        {
            return !field.IsReadOnlyField();
        }

        return false;
    }

    internal static bool IsAttachedPropertyField(this IFieldSymbol field)
    {
        if (field.IsObsolete())
        {
            return false;
        }

        return field.Type.Name.StartsWith("AttachedProperty") && !field.IsReadOnlyAttachedField();
    }

    internal static bool IsAcceptableStyledField(this IFieldSymbol field)
    {
        if (field.IsObsolete())
        {
            return false;
        }

        if (field.Type.Name.StartsWith("StyledProperty") || field.Type.Name.StartsWith("AttachedProperty"))
        {
            return !field.IsReadOnlyField();
        }

        return false;
    }

    internal static bool HasUnsupportedExternalValueType(this IFieldSymbol field)
    {
        var valueTypeName = field.Type.GetLastGenericArgument().GetFullTypeName();
        return valueTypeName is "global::Avalonia.Data.IBinding" or "global::Avalonia.Data.IBinding?";
    }

    internal static bool IsReadOnlyField(this IFieldSymbol field)
    {
        var controlType = field.ContainingType;
        var propertyName = field.GetMemberName();
        var symbol = controlType?.GetMembers(propertyName).FirstOrDefault();

        return symbol is not IPropertySymbol property || !property.HasPublicSetter();
    }

    internal static bool IsReadOnlyAttachedField(this IFieldSymbol field)
    {
        var controlType = field.ContainingType;
        var setterMethodName = "Set" + field.GetMemberName();
        var symbol = controlType?.GetMembers(setterMethodName).FirstOrDefault();

        return symbol is not IMethodSymbol method ||
               method.DeclaredAccessibility != Accessibility.Public ||
               !method.IsStatic;
    }

    internal static bool IsObsolete(this ISymbol symbol) =>
        symbol.GetAttributes().Any(static x => x.AttributeClass?.Name == nameof(ObsoleteAttribute));

    internal static string GetMemberName(this IFieldSymbol field)
    {
        if (field.AssociatedSymbol != null)
        {
            return RemoveTrailingProperty(field.AssociatedSymbol.Name);
        }

        return RemoveTrailingProperty(field.Name);
    }

    internal static string RemoveTrailingProperty(string source) =>
        source.EndsWith("Property") ? source.Substring(0, source.Length - "Property".Length) : source;

    internal static string GetFullTypeName(this ITypeSymbol symbol)
    {
        var fullName = symbol.ToDisplayString(FullyQualifiedGlobalFormat);

        if (!fullName.EndsWith("?") && symbol.NullableAnnotation == NullableAnnotation.Annotated)
        {
            fullName += "?";
        }

        return fullName;
    }

    internal static ITypeSymbol GetLastGenericArgument(this ITypeSymbol type)
    {
        if (type is INamedTypeSymbol namedType && namedType.TypeArguments.Length > 0)
        {
            return namedType.TypeArguments.Last();
        }

        return type;
    }

    internal static string RemoveIllegalFileNameCharacters(string fileName)
    {
        var invalidChars = new HashSet<char>(['<', '>', ':', '"', '/', '\\', '|', '?', '*']);
        return string.Concat(fileName.Select(c => invalidChars.Contains(c) || char.IsControl(c) ? '_' : c));
    }

    internal static string? CleanIdentifier(string name, bool @namespace = false)
    {
        name = name.Trim();

        if (string.IsNullOrEmpty(name))
        {
            return null;
        }

        var sb = new StringBuilder();
        if (!SyntaxFacts.IsIdentifierStartCharacter(name[0]))
        {
            sb.Append('_');
        }

        foreach (var ch in name)
        {
            if (SyntaxFacts.IsIdentifierPartCharacter(ch) || (@namespace && ch == '.'))
            {
                sb.Append(ch);
            }
        }

        var result = sb.ToString();
        if (SyntaxFacts.GetKeywordKind(result) != SyntaxKind.None)
        {
            result = '@' + result;
        }

        if (!@namespace)
        {
            return result;
        }

        var parts = result.Split('.');
        for (var i = 0; i < parts.Length; i++)
        {
            if (SyntaxFacts.GetKeywordKind(parts[i]) != SyntaxKind.None)
            {
                parts[i] = '@' + parts[i];
            }
        }

        return string.Join(".", parts);
    }

    internal static string BuildExtensionClassName(INamedTypeSymbol typeSymbol)
    {
        var parts = new List<string>();

        if (typeSymbol.ContainingNamespace is { IsGlobalNamespace: false } containingNamespace)
        {
            parts.AddRange(containingNamespace.ToDisplayString().Split('.'));
        }

        var typeParts = new Stack<string>();
        INamedTypeSymbol? current = typeSymbol;
        while (current != null)
        {
            typeParts.Push(current.Name.Split('`')[0]);
            current = current.ContainingType;
        }

        parts.AddRange(typeParts);

        return $"{CleanIdentifier(string.Join("_", parts))}_MarkupExtensions";
    }

    private static string GetMetadataName(ISymbol symbol)
    {
        var ns = symbol.ContainingNamespace?.ToDisplayString();
        return string.IsNullOrWhiteSpace(ns) ? symbol.Name : $"{ns}.{symbol.Name}";
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/Avalonia.Markup.Declarative.Tests.csproj">
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Headless" Version="12.0.0" />
    <PackageReference Include="Avalonia.Headless.XUnit" Version="12.0.0" />
    <PackageReference Include="Avalonia.Themes.Simple" Version="12.0.0" />
    <PackageReference Include="coverlet.collector" Version="8.0.1">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.4.0" />
    <PackageReference Include="Nut.Results.FluentAssertions" Version="2.1.1" />
    <PackageReference Include="xunit.v3" Version="3.2.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Avalonia.Markup.Declarative\Avalonia.Markup.Declarative.csproj" />
    <ProjectReference Include="..\Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
</file>

<file path="Avalonia.Markup.Declarative.Tests/AvaloniaTestBase.cs">
using Xunit;
using Avalonia.Headless;
using Avalonia.Platform;
using Avalonia.Themes.Simple;

[assembly: CollectionBehavior(DisableTestParallelization = true, MaxParallelThreads = 1)]

namespace Avalonia.Markup.Declarative.Tests;

public abstract class AvaloniaTestBase
{
    static AvaloniaTestBase()
    {
        if (Application.Current == null)
        {
            AppBuilder.Configure<Application>()
                .AfterSetup(b => b.Instance?.Styles.Add(new SimpleTheme()))
                .UseHeadless(new AvaloniaHeadlessPlatformOptions { UseHeadlessDrawing = true, FrameBufferFormat = PixelFormat.Bgra8888 })
                .UseViewInitializationStrategy(ViewInitializationStrategy.Lazy)
                .SetupWithoutStarting();
        }
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/BrushBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Media;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class BrushBindingTests : AvaloniaTestBase
{
    public class BrushViewModel : INotifyPropertyChanged
    {
        private IBrush _borderColor = Brushes.Red;

        public IBrush BorderColor
        {
            get => _borderColor;
            set { _borderColor = value; OnPropertyChanged(); }
        }

        public event PropertyChangedEventHandler? PropertyChanged;
        private void OnPropertyChanged([CallerMemberName] string? name = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
    }

    public class BorderBrushView : ViewBase<BrushViewModel>
    {
        public BorderBrushView(BrushViewModel vm) : base(vm) { }

        protected override object Build(BrushViewModel? vm) =>
            new Border()
                .Ref(out _border)
                .BorderBrush(vm!, x => x.BorderColor)
                .BorderThickness(3);

        public Border _border = null!;
    }

    [Fact]
    public void Border_BorderBrush_ExpressionBinding_Updates()
    {
        var vm = new BrushViewModel();
        var view = new BorderBrushView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal(Brushes.Red, view._border.BorderBrush);

        vm.BorderColor = Brushes.Green;
        Dispatcher.UIThread.RunJobs();

        Assert.Equal(Brushes.Green, view._border.BorderBrush);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/CommandBindingTests.cs">
using System.ComponentModel;
using System.Runtime.CompilerServices;
using Avalonia.Controls;
using Avalonia.Interactivity;
using Avalonia.Threading;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class CommandBindingTests : AvaloniaTestBase
{
    private record struct CmdObject(string MyProperty);

    private class Vm : INotifyPropertyChanged
    {
        private CmdObject _myObject = new("Start");
        public CmdObject MyObject
        {
            get => _myObject;
            set { if (_myObject.Equals(value)) return; _myObject = value; OnPropertyChanged(); }
        }

        public void MyCommand(object? param) => MyObject = new($"Param:{param is Vm}");

        public event PropertyChangedEventHandler? PropertyChanged;
        private void OnPropertyChanged([CallerMemberName] string? name = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
    }

    private class CommandView : ViewBase<Vm>
    {
        public CommandView(Vm vm) : base(vm) { _vm = vm; }
        private Vm _vm;

        protected override object Build(Vm? vm) =>
            new StackPanel()
                .Children(
                    new TextBlock().Ref(out _text).Text(vm!, x => x.MyObject.MyProperty),
                    new Button()
                        .Ref(out _button)
                        .Content("Execute Command")
                        .OnClick(_ => _vm.MyCommand(_vm))
                );

        public TextBlock _text = null!;
        public Button _button = null!;
    }

    [Fact]
    public void Button_Click_Updates_Text_Via_ViewModel()
    {
        var vm = new Vm();
        var view = new CommandView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("Start", view._text.Text);

        // Simulate click via raising the Click event
        view._button.RaiseEvent(new RoutedEventArgs(Button.ClickEvent));
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("Param:True", view._text.Text);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/ExpressionBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;
using FluentAssertions;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class ExpressionBindingTests : AvaloniaTestBase
{
    public class SeparatedViewState : INotifyPropertyChanged
    {
        public event PropertyChangedEventHandler? PropertyChanged;
        private string _stateProp = "Separate state property";

        public string StateProp
        {
            get => _stateProp;
            set
            {
                _stateProp = value;
                OnPropertyChanged();
            }
        }

        protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
    }

    public class ExpressionBindingTestView : ViewBase<SeparatedViewState>
    {
        public ExpressionBindingTestView(SeparatedViewState state) : base(state) { }

        protected override object Build(SeparatedViewState? vm) =>
            new StackPanel().Children(
                new TextBlock()
                    .Ref(out MyTextBlock)
                    .Text(vm!, x => x.StateProp)
            );

        public TextBlock MyTextBlock = null!;
    }

    [Fact]
    public void TextBlock_Binding_TextShouldBeUpdatedOnPropertyChanged()
    {
        var state = new SeparatedViewState();
        var view = new ExpressionBindingTestView(state);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        state.StateProp = "Notified!";
        Dispatcher.UIThread.RunJobs();

        var textBlock = view.MyTextBlock;
        Assert.NotNull(textBlock);
        textBlock.Text.Should().Be("Notified!");
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/ListBoxBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Data;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class ListBoxBindingTests : AvaloniaTestBase
{
    public class ListBoxViewModel : INotifyPropertyChanged
    {
        private string _selectedItem = "one";

        public string SelectedItem
        {
            get => _selectedItem;
            set { _selectedItem = value; OnPropertyChanged(); }
        }

        public List<string> Items { get; set; } = new() { "one", "two", "four" };

        public event PropertyChangedEventHandler? PropertyChanged;
        private void OnPropertyChanged([CallerMemberName] string? name = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
    }

    public class ListBoxSampleView : ViewBase<ListBoxViewModel>
    {
        public ListBoxSampleView(ListBoxViewModel vm) : base(vm) { }

        protected override object Build(ListBoxViewModel? vm) =>
            new StackPanel()
                .Children(
                    new ListBox()
                        .Ref(out _listBox)
                        .ItemsSource(vm!, x => x.Items)
                        .SelectedItem(vm!, x => x.SelectedItem, BindingMode.TwoWay),

                    new TextBlock().Ref(out _selectedText).Text(vm!, x => x.SelectedItem)
                );

        public ListBox _listBox = null!;
        public TextBlock _selectedText = null!;
    }

    [Fact]
    public void ListBox_SelectedItem_TwoWay_Works()
    {
        var vm = new ListBoxViewModel();
        var view = new ListBoxSampleView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("one", vm.SelectedItem);
        Assert.Equal("one", (string?)view._listBox.SelectedItem);

        vm.SelectedItem = "two";
        Dispatcher.UIThread.RunJobs();
        Assert.Equal("two", (string?)view._listBox.SelectedItem);

        view._listBox.SelectedItem = "four";
        Dispatcher.UIThread.RunJobs();
        Assert.Equal("four", vm.SelectedItem);
        Assert.Equal("four", view._selectedText.Text);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/NumericUpDownBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Data;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class NumericUpDownBindingTests : AvaloniaTestBase
{
    public class NumericViewModel : INotifyPropertyChanged
    {
        private decimal? _counter = 0m;

        public decimal? Counter
        {
            get => _counter;
            set { _counter = value; OnPropertyChanged(); }
        }

        public event PropertyChangedEventHandler? PropertyChanged;
        private void OnPropertyChanged([CallerMemberName] string? name = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
    }

    public class NumericView : ViewBase<NumericViewModel>
    {
        public NumericView(NumericViewModel vm) : base(vm) { }

        protected override object Build(NumericViewModel? vm) =>
            new NumericUpDown()
                .Ref(out _num)
                .Value(vm!, x => x.Counter, BindingMode.TwoWay);

        public NumericUpDown _num = null!;
    }

    [Fact]
    public void NumericUpDown_TwoWay_LambdaBinding_Works()
    {
        var vm = new NumericViewModel();
        var view = new NumericView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal(0m, vm.Counter);
        Assert.Equal((decimal?)0m, view._num.Value);

        vm.Counter = 5m;
        Dispatcher.UIThread.RunJobs();
        Assert.Equal((decimal?)5m, view._num.Value);

        view._num.Value = 12m;
        Dispatcher.UIThread.RunJobs();
        Assert.Equal(12m, vm.Counter);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/StyleBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Media;
using Avalonia.Threading;
using Avalonia.VisualTree;
using System.Linq;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class StyleBindingTests : AvaloniaTestBase
{
    public class TabVm
    {
        public string Title { get; }
        public bool Enabled { get; set; }
        public string Content => Title + " Content";

        public TabVm(string title, bool enabled = true)
        {
            Title = title;
            Enabled = enabled;
        }
    }

    public class TabsView : ViewBase
    {
        protected override StyleGroup? BuildStyles() =>
        [
            new Style<TabItem>()
                .IsEnabled(default(TabVm)!, x => x.Enabled)
                .Foreground(Brushes.YellowGreen)
        ];

        protected override object Build() =>
            new TabControl()
                .Ref(out _tabs)
                .ItemTemplate<TabVm>(tab => new TextBlock().Text(tab.Title))
                .ItemsSource(Tabs);

        public TabControl _tabs = null!;
        public List<TabVm> Tabs { get; set; } = new() { new("Tab1"), new("Disabled", false) };
    }

    [Fact]
    public void TabItem_IsEnabled_Binding_Via_Style_Works()
    {
        var view = new TabsView();
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var items = view._tabs.GetSelfAndVisualDescendants().OfType<TabItem>().ToList();
        Assert.True(items.Count >= 2);

        var first = items[0];
        var second = items[1];
        Assert.True(first.IsEnabled);
        Assert.False(second.IsEnabled);

        view.Tabs[1].Enabled = true;
        var itemsSrc = view.Tabs.ToList();
        view._tabs.ItemsSource = null;
        view._tabs.ItemsSource = itemsSrc;
        Dispatcher.UIThread.RunJobs();

        items = view._tabs.GetSelfAndVisualDescendants().OfType<TabItem>().ToList();
        second = items[1];
        Assert.True(second.IsEnabled);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/TemplateBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Controls.Primitives;
using Avalonia.Controls.Templates;
using Avalonia.Data;
using Avalonia.Threading;
using Avalonia.VisualTree;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class TemplateBindingTests : AvaloniaTestBase
{
    public class TemplateTestView : ViewBase
    {
        public static IControlTemplate MyControlTemplate { get; } = new FuncControlTemplate<Button>(
            (control, scope) => new FuncView<Button>(control, c =>
                new TextBox()
                    .Text(ContentControl.ContentProperty, c, BindingMode.OneWay)
            )
        );

        protected override object Build() =>
            new Button()
                .Content("Initial")
                .Template(MyControlTemplate)
                .Ref(out MyButton);

        public Button MyButton = null!;
    }

    [Fact]
    public void TextBox_Text_Bound_To_Button_Content()
    {
        var view = new TemplateTestView();
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var button = view.MyButton;
        Assert.NotNull(button);
        var textBox = button.GetSelfAndVisualDescendants().OfType<TextBox>().FirstOrDefault();
        Assert.NotNull(textBox);

        Assert.Equal("Initial", textBox.Text);
    }

    [Fact]
    public void Changing_Button_Content_Updates_TextBox_Text()
    {
        var view = new TemplateTestView();
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var button = view.MyButton;
        Assert.NotNull(button);
        var textBox = button.GetSelfAndVisualDescendants().OfType<TextBox>().FirstOrDefault();
        Assert.NotNull(textBox);

        button.Content = "Changed";
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("Changed", textBox.Text);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/TreeViewBindingTests.cs">
using System.Collections.ObjectModel;
using Avalonia.Controls;
using Avalonia.Controls.Templates;
using Avalonia.Data;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public class TreeViewBindingTests : AvaloniaTestBase
{
    public class Node(string name, IEnumerable<Node>? children = null)
    {
        public string Name { get; set; } = name;
        public ObservableCollection<Node> Children { get; set; } = new(children ?? []);
    }

    public class TreeViewModel : INotifyPropertyChanged
    {
        private Node? _selectedNode;

        public Node? SelectedNode
        {
            get => _selectedNode;
            set { _selectedNode = value; OnPropertyChanged(); OnPropertyChanged(nameof(SelectedNodeName)); }
        }

        public string SelectedNodeName => SelectedNode?.Name ?? "-";

        public ObservableCollection<Node> Nodes { get; set; } =
        [
            new Node("Animals", [new Node("Mammals", [new Node("Lion"), new Node("Cat"), new Node("Zebra")])])
        ];

        public event PropertyChangedEventHandler? PropertyChanged;
        private void OnPropertyChanged([CallerMemberName] string? name = null)
            => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
    }

    public class TreeViewSampleView : ViewBase<TreeViewModel>
    {
        public TreeViewSampleView(TreeViewModel vm) : base(vm) { }

        protected override object Build(TreeViewModel? vm) =>
            new StackPanel()
                .Children(
                    new TreeView()
                        .Ref(out _tree)
                        .ItemsSource(vm!.Nodes)
                        .ItemTemplate(
                            new FuncTreeDataTemplate<Node>((n, _) => new TextBlock().Text(n.Name), n => n.Children)
                        )
                        .SelectedItem(vm, x => x.SelectedNode!, BindingMode.TwoWay),

                    new TextBlock().Ref(out _text).Text(vm, x => x.SelectedNodeName)
                );

        public TreeView _tree = null!;
        public TextBlock _text = null!;
    }

    [Fact]
    public void TreeView_SelectedItem_TwoWay_Works()
    {
        var vm = new TreeViewModel();
        var view = new TreeViewSampleView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var lion = vm.Nodes[0].Children[0].Children[0];
        vm.SelectedNode = lion;
        Dispatcher.UIThread.RunJobs();
        Assert.Equal(lion, (Node?)view._tree.SelectedItem);
        Assert.Equal("Lion", view._text.Text);

        var cat = vm.Nodes[0].Children[0].Children[1];
        view._tree.SelectedItem = cat;
        Dispatcher.UIThread.RunJobs();
        Assert.Equal(cat, vm.SelectedNode);
        Assert.Equal("Cat", view._text.Text);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/BindingTests/ViewBaseBindingTests.cs">
using Avalonia.Controls;
using System.ComponentModel;
using System.Runtime.CompilerServices;
using Avalonia.Threading;

namespace Avalonia.Markup.Declarative.Tests.BindingTests;

public record struct MyObject(string MyProperty);

public class TestViewModel : INotifyPropertyChanged
{
    private MyObject _myObject = new("Initial");

    public MyObject MyObject
    {
        get => _myObject;
        set
        {
            if (_myObject == value) return;
            _myObject = value;
            OnPropertyChanged();
        }
    }

    public void MyCommand(object? commandParameter) => MyObject = new MyObject("New value");

    public event PropertyChangedEventHandler? PropertyChanged;
    protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
        => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
}

public class TestView(TestViewModel vm) : ViewBase<TestViewModel>(vm)
{
    protected override object Build(TestViewModel? vm) =>
        new StackPanel().Children(
            new TextBlock()
                .Text(vm!, x => x.MyObject.MyProperty)
                .Ref(out MyTextBlock)
        );

    public TextBlock MyTextBlock = null!;
}

public class ViewBaseBindingTests : AvaloniaTestBase
{
    [Fact]
    public void TextBlock_Binding_InitialValue()
    {
        var vm = new TestViewModel();
        var view = new TestView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("Initial", view.MyTextBlock.Text);
    }

    [Fact]
    public void TextBlock_Updates_When_ViewModel_Changes()
    {
        var vm = new TestViewModel();
        var view = new TestView(vm);
        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.Equal("Initial", view.MyTextBlock.Text);

        vm.MyCommand(null);
        Dispatcher.UIThread.RunJobs();

        Assert.Equal(vm.MyObject.MyProperty, view.MyTextBlock.Text);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/ControlsTests/AttachedPropertySetterTests.cs">
using Avalonia.Controls;

namespace Avalonia.Markup.Declarative.Tests.ControlsTests;

public class AttachedPropertySetterTests : AvaloniaTestBase
{
    [Fact]
    public void AttachedProperty_Value_Setters_Are_Generated()
    {
        var control = new Border()
            .Grid_Row(2)
            .Canvas_Left(42d);

        Assert.Equal(2, Grid.GetRow(control));
        Assert.Equal(42d, Canvas.GetLeft(control));
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/ControlsTests/ExternalPropertySetTests.cs">
using Avalonia.Controls;
using Avalonia.Threading;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative.Tests.ControlsTests;

public class SliderWithLabelViewModel : INotifyPropertyChanged
{
    private double _value;
    private string _label = "";

    public double Value
    {
        get => _value;
        set { _value = value; OnPropertyChanged(); }
    }

    public string Label
    {
        get => _label;
        set { _label = value; OnPropertyChanged(); }
    }

    public event PropertyChangedEventHandler? PropertyChanged;
    private void OnPropertyChanged([CallerMemberName] string? name = null)
        => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
}

public class SliderWithLabel : ViewBase<SliderWithLabelViewModel>
{
    public SliderWithLabel(SliderWithLabelViewModel vm) : base(vm) { }

    protected override object Build(SliderWithLabelViewModel? vm) =>
        new StackPanel()
            .Children(
                new TextBlock()
                    .Ref(out _labelTextBlock)
                    .Text(vm!, x => x.Label),

                new Slider()
                    .Ref(out _slider)
                    .Value(vm!, x => x.Value, Avalonia.Data.BindingMode.TwoWay)
            );

    private TextBlock _labelTextBlock = null!;
    private Slider _slider = null!;

    public string GetRealLabelValue() => _labelTextBlock.Text ?? "Not set";
}

public class ExternalPropertySetTest(ITestOutputHelper testOutputHelper) : AvaloniaTestBase
{
    [Fact]
    public void SliderWithLabel_Label_Binding_Applied()
    {
        var vm = new SliderWithLabelViewModel { Label = "Hello world", Value = 1 };
        var view = new SliderWithLabel(vm);

        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var realLabelValue = view.GetRealLabelValue();
        Assert.Equal("Hello world", realLabelValue);
    }

    [Fact]
    public void SliderWithLabel_Value_TwoWay_Binding_Works()
    {
        var vm = new SliderWithLabelViewModel { Label = "Test", Value = 1 };
        var view = new SliderWithLabel(vm);

        var window = new Window { Content = view };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        vm.Value = 100;
        Dispatcher.UIThread.RunJobs();

        Assert.Equal(100, vm.Value);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/ControlsTests/ModalHostTests.cs">
using Avalonia.Controls;
using Avalonia.Threading;

namespace Avalonia.Markup.Declarative.Tests.ControlsTests;

public class ModalHostTests : AvaloniaTestBase
{
    public class ModalHost : ViewBase
    {
        internal Panel? HostPanel;

        public Control? MainContent { get; set; }

        public bool WasMainContentNullDuringBuild { get; private set; }

        protected override object Build()
        {
            var children = new List<Control>();

            WasMainContentNullDuringBuild = MainContent is null;
            if (MainContent is not null)
                children.Add(MainContent);

            children.Add(new Panel());

            var hostPanel = new Panel()
                .Children(children.ToArray());

            HostPanel = hostPanel;
            return hostPanel;
        }
    }

    [Fact]
    public void Build_is_called_lazily_on_visual_tree_attachment()
    {
        var host = new ModalHost();
        // MainContent set BEFORE attaching to visual tree
        host.MainContent = new TextBlock();

        // Build not yet called
        Assert.Null(host.HostPanel);

        // Attach to visual tree triggers Build
        var window = new Window { Content = host };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        Assert.NotNull(host.HostPanel);
        // MainContent was set before Build, so it should be included
        Assert.False(host.WasMainContentNullDuringBuild);
        Assert.Equal(2, host.HostPanel!.Children.Count); // MainContent + Modals panel
    }

    [Fact]
    public void Children_not_updated_when_setting_MainContent_after_build()
    {
        var host = new ModalHost();
        var window = new Window { Content = host };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        // Build was called without MainContent
        Assert.True(host.WasMainContentNullDuringBuild);
        Assert.NotNull(host.HostPanel);
        Assert.Single(host.HostPanel!.Children);

        // Setting MainContent after Build doesn't update children
        host.MainContent = new TextBlock();
        Assert.Single(host.HostPanel.Children);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/ControlsTests/MyCustomControlBindingTests.cs">
using Avalonia.Controls;
using Avalonia.Data;
using Avalonia.Threading;

namespace Avalonia.Markup.Declarative.Tests.ControlsTests;

public class MyCustomControlBindingTests : AvaloniaTestBase
{
    public class TestCustomControl : ViewBase
    {
        private string? _newValue;
        public static readonly DirectProperty<TestCustomControl, string?> NewValueProperty =
            AvaloniaProperty.RegisterDirect<TestCustomControl, string?>(nameof(NewValue), o => o.NewValue, (o, v) => o.NewValue = v);

        public string? NewValue
        {
            get => _newValue;
            set { SetAndRaise(NewValueProperty, ref _newValue, value); UpdateCanSave(); }
        }

        private string? _savedValue;
        public static readonly DirectProperty<TestCustomControl, string?> SavedValueProperty =
            AvaloniaProperty.RegisterDirect<TestCustomControl, string?>(nameof(SavedValue), o => o.SavedValue, (o, v) => o.SavedValue = v);

        public string? SavedValue
        {
            get => _savedValue;
            set { SetAndRaise(SavedValueProperty, ref _savedValue, value); UpdateCanSave(); }
        }

        private bool _canSave;
        public static readonly DirectProperty<TestCustomControl, bool> CanSaveProperty =
            AvaloniaProperty.RegisterDirect<TestCustomControl, bool>(nameof(CanSave), o => o.CanSave);

        public bool CanSave
        {
            get => _canSave;
            private set => SetAndRaise(CanSaveProperty, ref _canSave, value);
        }

        private void UpdateCanSave() => CanSave = SavedValue != NewValue;

        public void Save() => SavedValue = NewValue;
        public void Cancel() => NewValue = SavedValue;

        protected override object Build() =>
            new Grid()
                .Children(
                    new TextBox().Ref(out TextBox).Text(NewValueProperty, this, BindingMode.TwoWay),
                    new Button().Ref(out CancelButton).IsEnabled(CanSaveProperty, this),
                    new Button().Ref(out SaveButton).IsEnabled(CanSaveProperty, this)
                );

        public TextBox TextBox = null!;
        public Button SaveButton = null!;
        public Button CancelButton = null!;
    }

    public class Host : ViewBase
    {
        protected override object Build() => new TestCustomControl().Ref(out Control);
        public TestCustomControl Control = null!;
    }

    [Fact]
    public void CustomControl_AvaloniaPropertyBindings_Work()
    {
        var host = new Host();
        var window = new Window { Content = host };
        window.Show();
        Dispatcher.UIThread.RunJobs();

        var control = host.Control;

        control.NewValue = "A";
        control.SavedValue = "A";
        Dispatcher.UIThread.RunJobs();
        Assert.False(control.CanSave);

        control.NewValue = "B";
        Dispatcher.UIThread.RunJobs();
        Assert.True(control.CanSave);

        control.Save();
        Dispatcher.UIThread.RunJobs();
        Assert.Equal(control.SavedValue, control.NewValue);
        Assert.False(control.CanSave);

        control.SavedValue = "C";
        Dispatcher.UIThread.RunJobs();
        Assert.True(control.CanSave);

        control.Cancel();
        Dispatcher.UIThread.RunJobs();
        Assert.Equal("C", control.NewValue);
        Assert.False(control.CanSave);
    }
}
</file>

<file path="Avalonia.Markup.Declarative.Tests/GlobalSuppressions.cs">
// This file is used by Code Analysis to maintain SuppressMessage
// attributes that are applied to this project.
// Project-level suppressions either have no target or are given
// a specific target and scoped to a namespace, type, member, etc.

using System.Diagnostics.CodeAnalysis;

[assembly: SuppressMessage("Usage", "xUnit1012:Null should only be used for nullable parameters", Justification = "<Pending>", Scope = "member", Target = "~M:Avalonia.Markup.Declarative.Tests.SelectorParserHelperTests.GetTypeNameFromSelectorString_ReturnsExpected(System.String,System.String)")]
</file>

<file path="Avalonia.Markup.Declarative.Tests/HelpersTests/SelectorParserHelperTests.cs">
using Avalonia.Markup.Declarative.Helpers;

namespace Avalonia.Markup.Declarative.Tests.HelpersTests;

public class SelectorParserHelperTests
{
    [Theory]
    [InlineData("Button", "Button")]
    [InlineData("TextBlock.someClass", "TextBlock")]
    [InlineData(".someClass", null)]
    [InlineData("#someId", null)]
    [InlineData(":is(TextBlock)", "TextBlock")]
    [InlineData("Button:is(TextBlock)", "TextBlock")]
    [InlineData("Button#id.class", "Button")]
    [InlineData("Button.child", "Button")]
    [InlineData("", null)]
    [InlineData("   ", null)]
    [InlineData(null, null)]
    [InlineData("Panel:foo(Button)", "Button")]
    [InlineData("Panel:is(Button):is(TextBlock)", "TextBlock")]
    [InlineData("Button:is(TextBlock):is(Panel)", "Panel")]
    [InlineData("Button#id.class:is(Panel)", "Panel")]
    [InlineData("#SideBar :is(TextBlock)#title", "TextBlock")]
    [InlineData("StackPanel#SideBar :is(TextBlock)#title", "TextBlock")]
    public void GetTypeNameFromSelectorString_ReturnsExpected(string selector, string expected)
    {
        var result = SelectorParserHelper.GetTypeNameFromSelectorString(selector);
        Assert.Equal(expected, result);
    }
}
</file>

<file path="Avalonia.Markup.Declarative/Avalonia.Markup.Declarative.csproj">
<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net10.0;net9.0;net8.0</TargetFrameworks>
		<IsPackable>true</IsPackable>
		<Nullable>enable</Nullable>
		<LangVersion>latest</LangVersion>
		<GeneratePackageOnBuild>False</GeneratePackageOnBuild>

		<Authors>Avalonia Team</Authors>
		<PackageProjectUrl>https://github.com/AvaloniaUI/Avalonia.Markup.Declarative</PackageProjectUrl>
		<RepositoryUrl>https://github.com/AvaloniaUI/Avalonia.Markup.Declarative</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<Description>The set of base classes and extension methods over Avalonia's controls to define views/markup with C# code instead of XAML.</Description>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<PackageIcon>icon.png</PackageIcon>
		<PackageTags>avalonia;avaloniaui;mvvm;mvp;csharpmarjup;android;ios;mac;forms;wpf;net;netstandard;net461;uwp;xamarin</PackageTags>
		<PackageReadmeFile>README.md</PackageReadmeFile>
	</PropertyGroup>
	
	<ItemGroup>
		<InternalsVisibleTo Include="Avalonia.Markup.Declarative.Tests" />
	</ItemGroup>

	<ItemGroup>
		<None Include="icon.png" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="System.Collections.Immutable" Version="10.0.5" />
		<PackageReference Include="Avalonia" Version="12.0.0" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj" ReferenceOutputAssembly="true">
			<PrivateAssets>all</PrivateAssets>
		</ProjectReference>
	</ItemGroup>

	<ItemGroup>
		<None Remove="$(OutputPath)/$(AssemblyName).SourceGenerator.dll" />
	</ItemGroup>

	<ItemGroup>
		<!-- Package the generator in the analyzer directory of the nuget package -->
		<None Include="../Avalonia.Markup.Declarative.SourceGenerator/bin/$(Configuration)/netstandard2.0/Avalonia.Markup.Declarative.SourceGenerator.dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
	</ItemGroup>

	<ItemGroup>
		<None Include="readme.txt" Pack="true" PackagePath="." />
	</ItemGroup>
	<ItemGroup>
		<None Include="README.md" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
		<Folder Include="ControlExtensions.Generated\" />
	</ItemGroup>
</Project>
</file>

<file path="Avalonia.Markup.Declarative/CommonExtensions/AppBuilderExtensions.cs">
using Avalonia.Controls;
using System;

namespace Avalonia.Markup.Declarative;

/// <summary>
/// Provides extension methods for configuring an <see cref="AppBuilder"/>.
/// </summary>
public static class AppBuilderExtensions
{
    private static IComponentControlFactory? _componentControlFactory;
    private static IServiceProvider? _serviceProvider;
    private static ViewInitializationStrategy _defaultViewInitializationStrategy = ViewInitializationStrategy.Lazy;

    /// <summary>
    /// Gets the current <see cref="IServiceProvider"/> instance.
    /// </summary>
    internal static IServiceProvider? ServiceProvider => _serviceProvider;

    /// <summary>
    /// Gets the current <see cref="IComponentControlFactory"/> instance.
    /// </summary>
    internal static IComponentControlFactory? ComponentControlFactory => _componentControlFactory;

    /// <summary>
    /// Gets the default <see cref="ViewInitializationStrategy"/> used by views when not explicitly specified.
    /// </summary>
    internal static ViewInitializationStrategy DefaultViewInitializationStrategy => _defaultViewInitializationStrategy;

    /// <summary>
    /// Configures the <see cref="AppBuilder"/> to use the specified <see cref="IServiceProvider"/>.
    /// </summary>
    /// <param name="appBuilder">The <see cref="AppBuilder"/> to configure.</param>
    /// <param name="serviceProvider">The <see cref="IServiceProvider"/> to use.</param>
    /// <returns>The configured <see cref="AppBuilder"/>.</returns>
    public static AppBuilder UseServiceProvider(this AppBuilder appBuilder, IServiceProvider serviceProvider)
    {
        _serviceProvider = serviceProvider;
        return appBuilder;
    }


    /// <summary>
    /// Configures the application to use a custom factory for creating component controls.
    /// </summary>
    /// <remarks>Use this method to provide a custom control creation strategy for components. This is useful
    /// when integrating with dependency injection or customizing control instantiation.</remarks>
    /// <param name="appBuilder">The application builder to configure.</param>
    /// <param name="factory">A delegate that creates a control instance for the specified component type. Cannot be null.</param>
    /// <returns>The configured application builder instance.</returns>
    public static AppBuilder UseComponentControlFactory(this AppBuilder appBuilder, Func<Type, Control> factory)
    {
        _componentControlFactory = new DelegateControlFactory(factory);
        return appBuilder;
    }

    private class DelegateControlFactory(Func<Type, Control> factory) : IComponentControlFactory
    {
        public TControl CreateControlInstance<TControl>() where TControl : Control
        {
            return (TControl)factory(typeof(TControl));
        }
    }

    /// <summary>
    /// Configures the default initialization strategy for views.
    /// </summary>
    /// <param name="appBuilder">The <see cref="AppBuilder"/> to configure.</param>
    /// <param name="strategy">The <see cref="ViewInitializationStrategy"/> to use as default for all views.</param>
    /// <returns>The configured <see cref="AppBuilder"/>.</returns>
    public static AppBuilder UseViewInitializationStrategy(this AppBuilder appBuilder, ViewInitializationStrategy strategy)
    {
        _defaultViewInitializationStrategy = strategy;
        return appBuilder;
    }

    /// <summary>
    /// Enables or disables hot reload functionality for the application.
    /// </summary>
    /// <param name="appBuilder">The <see cref="AppBuilder"/> to configure.</param>
    /// <param name="enable">A value indicating whether to enable hot reload. Defaults to <c>true</c>.</param>
    /// <returns>The configured <see cref="AppBuilder"/>.</returns>
    public static AppBuilder UseHotReload(this AppBuilder appBuilder, bool enable = true)
    {
        if (enable)
        {
            HotReloadManager.Activate();
        }
        else
        {
            HotReloadManager.Deactivate();
        }
        return appBuilder;
    }
}
</file>

<file path="Avalonia.Markup.Declarative/CommonExtensions/BindingExtensions.cs">
using Avalonia.Data;
using Avalonia.Data.Converters;

namespace Avalonia.Markup.Declarative;

public static class BindingExtensions
{
    public static TBinding Converter<TBinding>(this TBinding binding, IValueConverter converter)
        where TBinding : Binding
    {
        binding.Converter = converter;
        return binding;
    }

}
</file>

<file path="Avalonia.Markup.Declarative/CommonExtensions/ControlEventExtensions.cs">
using System;

namespace Avalonia.Markup.Declarative;

public static class ControlEventExtensions
{
    public static TControl _setEvent<TControl, THandler>(this TControl control, THandler handler, Action<THandler> subscribe)
        where TControl : AvaloniaObject
    {
        subscribe?.Invoke(handler);
        return control;
    }
}
</file>

<file path="Avalonia.Markup.Declarative/CommonExtensions/ConverterExtensions.cs">
using Avalonia.Data.Converters;
using System.Globalization;

namespace Avalonia.Markup.Declarative;

public static class ConverterExtensions
{
    public static TOut TryConvert<TIn, TOut>(this FuncValueConverter<TIn, TOut> converter, TIn value)
    {
#pragma warning disable CS8600 // Converting null literal or possible null value to non-nullable type.
#pragma warning disable CS8603 // Possible null reference return.
        return (TOut)converter.Convert(value, typeof(TOut), null, CultureInfo.InvariantCulture);
#pragma warning restore CS8603 // Possible null reference return.
#pragma warning restore CS8600 // Converting null literal or possible null value to non-nullable type.
    }
}
</file>

<file path="Avalonia.Markup.Declarative/ControlPropertyExtensions.cs">
using Avalonia.Controls;
using Avalonia.Controls.Primitives;
using Avalonia.Controls.Templates;
using Avalonia.Data;
using Avalonia.Data.Converters;
using Avalonia.Media;
using Avalonia.Styling;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using System.Linq.Expressions;
using System.Runtime.CompilerServices;
using System.Windows.Input;

namespace Avalonia.Markup.Declarative;

public static class ControlPropertyExtensions
{
    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    public static TControl _set<TControl>(this TControl control, Action setAction)
    {
        setAction();
        return control;
    }

    /// <summary>
    /// Creates a compiled binding from an expression and applies it to an Avalonia property.
    /// </summary>
    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    public static TControl _setCompiledBinding<TControl, TViewModel, TValue>(
        this TControl control,
        AvaloniaProperty avaloniaProperty,
        TViewModel source,
        Expression<Func<TViewModel, TValue>> getter,
        BindingMode? mode = null,
        IValueConverter? converter = null)
        where TControl : AvaloniaObject
    {
        var binding = CompiledBinding.Create(getter,
            source: source,
            mode: mode ?? BindingMode.Default,
            converter: converter);

        control.Bind(avaloniaProperty, binding);
        return control;
    }

    /// <summary>
    /// Binds one Avalonia property to another on a given source object.
    /// </summary>
    public static TControl _setPropertyBinding<TControl>(this TControl control,
        AvaloniaProperty avaloniaProperty,
        AvaloniaProperty propertyToBindTo,
        AvaloniaObject source,
        BindingMode? bindingMode = null,
        IValueConverter? converter = null)
        where TControl : AvaloniaObject
    {
        var binding = new Binding()
        {
            Source = source,
            Path = propertyToBindTo.Name,
            Mode = bindingMode ?? BindingMode.Default,
            Converter = converter
        };

        control[!avaloniaProperty] = binding;
        return control;
    }

    public static TElement DataContext<TElement, TDataContext>(
        this TElement control,
        TDataContext value,
        out TDataContext dataContext)
        where TElement : StyledElement where TDataContext : class
    {
        dataContext = value;
        control.DataContext = value;
        return control;
    }

    public static Brush ToBrush(this Color color) => new SolidColorBrush(color);

    public static TElement Dock<TElement>(this TElement control, Dock dock)
        where TElement : Control
    {
        DockPanel.SetDock(control, dock);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_Column (in xaml: Grid.Column) extension 
    /// </summary>
    /// <typeparam name="TElement"></typeparam>
    /// <param name="control">Control for positioning</param>
    /// <param name="value">Grid.Column value</param>
    /// <returns></returns>
	public static TElement Col<TElement>(this TElement control, int value)
        where TElement : Control
    {
        Grid.SetColumn(control, value);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_Row (in xaml: Grid.Row) extension 
    /// </summary>
    /// <typeparam name="TElement">Control type</typeparam>
    /// <param name="control">Control for positioning</param>
    /// <param name="value">Grid.Row value</param>
    /// <returns></returns>
    public static TElement Row<TElement>(this TElement control, int value)
        where TElement : Control
    {
        Grid.SetRow(control, value);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_ColumnDefinitions (in xaml: Grid.ColumnDefinitions) extension 
    /// </summary>
    /// <typeparam name="TElement">Grid</typeparam>
    /// <param name="control">Grid control</param>
    /// <param name="value">Grid.ColumnDefinitions value</param>
    /// <returns></returns>
	public static TElement Cols<TElement>(this TElement control, ColumnDefinitions value)
        where TElement : Grid
    {
        control.ColumnDefinitions = value;
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_RowDefinitions (in xaml: Grid.RowDefinitions) extension 
    /// </summary>
    /// <typeparam name="TElement">Grid</typeparam>
    /// <param name="control">Grid control</param>
    /// <param name="value">Grid.RowDefinitions value</param>
    /// <returns></returns>
	public static TElement Rows<TElement>(this TElement control, RowDefinitions value)
        where TElement : Grid
    {
        control.RowDefinitions = value;
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_ColumnDefinitions (in xaml: Grid.ColumnDefinitions) extension 
    /// </summary>
    /// <typeparam name="TElement">Grid</typeparam>
    /// <param name="control">Grid control</param>
    /// <param name="value">String representing ColumnDefinitions i.e. "0,*,30,Auto" </param>
    /// <returns></returns>
	public static TElement Cols<TElement>(this TElement control, string value)
        where TElement : Grid
    {
        control.ColumnDefinitions = ColumnDefinitions.Parse(value);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_RowDefinitions (in xaml: Grid.RowDefinitions) extension 
    /// </summary>
    /// <typeparam name="TElement">Grid</typeparam>
    /// <param name="control">Grid control</param>
    /// <param name="value">String representing RowDefinitions i.e. "0,*,30,Auto" </param>
    /// <returns></returns>
	public static TElement Rows<TElement>(this TElement control, string value)
        where TElement : Grid
    {
        control.RowDefinitions = RowDefinitions.Parse(value);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_ColumnSpan (in xaml: Grid.ColumnSpan) extension 
    /// </summary>
    /// <typeparam name="TElement">Control Type</typeparam>
    /// <param name="control">Control for positioning</param>
    /// <param name="value">Grid.ColumnSpan value</param>
    /// <returns></returns>
	public static TElement ColSpan<TElement>(this TElement control, int value)
        where TElement : Control
    {
        Grid.SetColumnSpan(control, value);
        return control;
    }

    /// <summary>
    /// It's a shortcut for Grid_RowSpan (in xaml: Grid.RowSpan) extension 
    /// </summary>
    /// <typeparam name="TElement">Control type</typeparam>
    /// <param name="control">Control for positioning</param>
    /// <param name="value">Grid.RowSpan value</param>
    /// <returns></returns>
	public static TElement RowSpan<TElement>(this TElement control, int value)
        where TElement : Control
    {
        Grid.SetRowSpan(control, value);
        return control;
    }

    public static TPanel Children<TPanel>(this TPanel container, params Control[] children)
        where TPanel : Panel
    {
        foreach (var child in children)
            container.Children.Add(child);
        return container;
    }

    public static TItemsControl Items<TItemsControl>(this TItemsControl container, params AvaloniaObject[] items)
        where TItemsControl : ItemsControl
    {
        if (container.Items is IList itemsCollection)
            foreach (var item in items)
                itemsCollection.Add(item);
        return container;
    }

    public static TabControl ItemTemplate<TItem>(this TabControl control, Func<TItem, Control> build) =>
        ItemTemplate<TItem, TabControl>(control, build);

    public static SelectingItemsControl ItemTemplate<TItem>(this SelectingItemsControl control,
        Func<TItem, Control> build) =>
        ItemTemplate<TItem, SelectingItemsControl>(control, build);

    public static TItemsControl ItemTemplate<TItem, TItemsControl>(this TItemsControl control,
        Func<TItem, Control> build)
        where TItemsControl : ItemsControl
    {
        control.ItemTemplate = control.ItemTemplate = new FuncDataTemplate<TItem>((val, _) => build(val));
        return control;
    }

    public static MenuFlyout ItemTemplate<TItem>(this MenuFlyout control, Func<TItem, Control> build)
    {
        control.ItemTemplate = new FuncDataTemplate<TItem>((val, _) => build(val));
        return control;
    }
    public static MenuItem ItemTemplate<TItem>(this MenuItem control, Func<TItem, Control> build)
    {
        control.ItemTemplate = new FuncDataTemplate<TItem>((val, _) => build(val));
        return control;
    }
    public static Menu ItemTemplate<TItem>(this Menu control, Func<TItem, Control> build)
    {
        control.ItemTemplate = new FuncDataTemplate<TItem>((val, _) => build(val));
        return control;
    }

    public static TItemsControl ItemsPanel<TItemsControl>(this TItemsControl control, Panel panel)
        where TItemsControl : ItemsControl
    {
        control.ItemsPanel = new PanelTemplate(panel);
        return control;
    }

    record PanelTemplate(Panel panel) : ITemplate<Panel?>
    {
        public Panel Build() => panel;
        object ITemplate.Build() => throw new NotImplementedException();
    }

    public static TElement With<TElement>(this TElement control, Action<TElement> process)
    {
        process?.Invoke(control);
        return control;
    }

    public static TElement Name<TElement>(this TElement control, string name, INameScope ns)
        where TElement : Control
    {
        ns?.Register(name, control);
        control.Name = name;
        return control;
    }

    public static TElement Styles<TElement>(this TElement control, params Style[] styles)
        where TElement : Control
    {
        foreach (var style in styles)
            control.Styles.Add(style);

        return control;
    }

    public static TElement Styles<TElement>(this TElement control, IEnumerable<Style> styles)
        where TElement : Control
    {
        foreach (var style in styles)
            control.Styles.Add(style);

        return control;
    }

    public static TElement Classes<TElement>(this TElement control, string className, [CallerLineNumber] int line = 0,
        [CallerMemberName] string? caller = null)
        where TElement : Control
    {
        control.Classes.Add(className);
        return control;
    }

    public static TElement BindClass<TElement, TViewModel>(this TElement control,
        TViewModel source,
        Expression<Func<TViewModel, bool>> getter,
        string className)
        where TElement : Control
    {
        var binding = CompiledBinding.Create(getter, source: source);
        control.BindClass(className, binding, null!);
        return control;
    }

    public static TElement BindClass<TElement>(this TElement control, bool value, string className,
        object? bindingSource = null, [CallerLineNumber] int line = 0, [CallerMemberName] string? caller = null,
        [CallerArgumentExpression(nameof(value))] string? ps = null)
        where TElement : Control
    {
        var binding = new ReflectionBinding(ps ?? string.Empty);

        if (bindingSource != null)
            binding.Source = bindingSource;

        control.BindClass(className, binding, null!);
        return control;
    }

    public static StackTrace GetDeeperStackTrace(int depth) =>
        depth > 0 ? GetDeeperStackTrace(depth - 1) : new StackTrace(0, true);


    public static TElement DataTemplates<TElement>(this TElement control, params IDataTemplate[] dataTemplate)
        where TElement : Control
    {
        foreach (var template in dataTemplate)
            control.DataTemplates.Add(template);
        return control;
    }

    public static TElement SetProp<TElement, TValue>(this TElement control, Avalonia.AvaloniaProperty property,
        TValue value)
        where TElement : Control
    {
        if (value is ReflectionBinding binding)
        {
            control[!property] = binding;
        }
        else
        {
            control[property] = value;
        }

        return control;
    }

    public static TElement ToolTip<TElement, TValue>(this TElement control, TValue value)
        where TElement : Control
    {
        var prop = Avalonia.Controls.ToolTip.TipProperty;
        if (value is ReflectionBinding binding)
        {
            control[!prop] = binding;
        }
        else
        {
            control[prop] = value;
        }

        return control;
    }

    /// <summary>
    /// Adds flyout to button and activates it on button click
    /// </summary>
    /// <typeparam name="TElement"></typeparam>
    /// <param name="control">target button</param>
    /// <param name="flyout">flyout to activate</param>
    /// <returns></returns>
    public static TElement AddFlyoutOnClick<TElement>(this TElement control, FlyoutBase flyout)
        where TElement : Button
    {
        control.Click += (o, e) => flyout.ShowAt(control);
        return control;
    }

    /// <summary>
    /// Adds a menu item to the specified menu flyout.
    /// </summary>
    /// <typeparam name="TElement">The type of the menu flyout element.</typeparam>
    /// <param name="menuFlyout">The menu flyout to which the item will be added.</param>
    /// <param name="menuItem">The menu item to be added to the flyout.</param>
    /// <returns>The menu flyout with the added item.</returns>
    public static TElement AddItem<TElement>(this TElement menuFlyout, MenuItem menuItem)
        where TElement : MenuFlyout
    {
        menuFlyout.Items.Add(menuItem);
        return menuFlyout;
    }

    /// <summary>
    /// Adds item to MenuFlyout
    /// </summary>
    /// <typeparam name="TElement">MenuFlyout type</typeparam>
    /// <param name="menuFlyout">Target MenuFlyout control</param>
    /// <param name="text">Item text</param>
    /// <param name="command">Item command</param>
    /// <param name="commandParameter">Command parameter</param>
    /// <returns></returns>
    public static TElement AddItem<TElement>(this TElement menuFlyout, string text, ICommand command,
        object? commandParameter = null)
        where TElement : MenuFlyout
    {
        var item = new MenuItem() { Header = text, Command = command };
        if (commandParameter != null)
            item.CommandParameter = commandParameter;

        menuFlyout.Items.Add(item);
        return menuFlyout;
    }

    /// <summary>
    /// Sets control instance reference to field so it can be accessed later in Markup
    /// </summary>
    /// <typeparam name="TElement">Control Type</typeparam>
    /// <param name="control">Control instance</param>
    /// <param name="field">field that will accept reference to control</param>
    /// <returns></returns>
    public static TElement Ref<TElement>(this TElement control, out TElement field)
    {
        field = control;
        return control;
    }
}
</file>

<file path="Avalonia.Markup.Declarative/FuncView.cs">
using System;
using Avalonia.Controls;

namespace Avalonia.Markup.Declarative;

public class FuncView<TViewModel>(TViewModel model, Func<TViewModel, Control> build) : ViewBase<TViewModel>(model)
    where TViewModel : class
{
    protected override object Build(TViewModel? vm) => build.Invoke(vm!);
}
</file>

<file path="Avalonia.Markup.Declarative/GenerateMarkupExtensionsForAssemblyAttribute.cs">
using System;

namespace Avalonia.Markup.Declarative;

[AttributeUsage(AttributeTargets.Assembly, AllowMultiple = true)]
public sealed class GenerateMarkupExtensionsForAssemblyAttribute(Type anchorType) : Attribute
{
    public Type AnchorType { get; } = anchorType;
}
</file>

<file path="Avalonia.Markup.Declarative/Helpers/SelectorParserHelper.cs">
using System.Text.RegularExpressions;
using Avalonia.Styling;

namespace Avalonia.Markup.Declarative.Helpers;

internal static class SelectorParserHelper
{
    public static string? GetTypeNameFromSelector(this Selector selector) =>
        GetTypeNameFromSelectorString(selector.ToString());

    public static string? GetTypeNameFromSelectorString(string selectorString)
    {
        if (string.IsNullOrWhiteSpace(selectorString))
        {
            return null;
        }

        // Regular expression for finding type names.
        // It searches for:
        // 1. A word that is not preceded by '.', '#', or ':' (a direct type name, e.g., "Button", "TextBlock").
        //    Uses negative lookbehind `(?<![.#:])`
        // 2. Or a word that is inside `:is(...)` (e.g., ":is(TextBlock)").
        //    Uses lookbehind `(?<=:is\()` and lookahead `(?=\))`
        // Combines them using `|` (OR) and uses the named group `(?<typeName>...)` to capture the name.
        string pattern = @"(?<![.#:])\b(?<typeName>[A-Za-z_][A-Za-z0-9_]*)\b|:is\((?<typeName>[A-Za-z_][A-Za-z0-9_]*)\)";

        MatchCollection matches = Regex.Matches(selectorString, pattern);

        // If there are matches, take the last one and extract the type name from it.
        if (matches.Count > 0)
        {
            // Iterate in reverse order to find the last non-empty type name.
            // This is important because the same position can be captured by both parts of the OR in the pattern,
            // but only one of the 'typeName' groups will have a value.
            for (int i = matches.Count - 1; i >= 0; i--)
            {
                Match match = matches[i];
                // Check the named group 'typeName'.
                // If it contains a value (i.e., one of the OR parts matched), return it.
                string typeName = match.Groups["typeName"].Value;
                if (!string.IsNullOrEmpty(typeName))
                {
                    return typeName.Trim();
                }
            }
        }

        return null;
    }
}
</file>

<file path="Avalonia.Markup.Declarative/HotReloadManager.cs">
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Linq;

[assembly: System.Reflection.Metadata.MetadataUpdateHandler(typeof(Avalonia.Markup.Declarative.HotReloadManager))]

namespace Avalonia.Markup.Declarative;

public static class HotReloadManager
{
    private static readonly ConcurrentDictionary<Type, HashSet<IReloadable>> Instances = new();

    public static event Action<Type[]?>? HotReloaded;

    public static bool IsEnabled { get; private set; } = true;

    public static void Activate() =>
        IsEnabled = true;

    public static void Deactivate() =>
        IsEnabled = false;

    private static void OnHotReloaded(Type[]? types) =>
        HotReloaded?.Invoke(types);

    public static void ClearCache(Type[]? types)
    {
        Console.WriteLine("ClearCache for types: " + PrintTypes(types));
    }

    public static void UpdateApplication(Type[]? types)
    {
        if (IsEnabled)
            ReloadInstances(types);

        OnHotReloaded(types);
    }

    private static void ReloadInstances(Type[]? types)
    {
        Console.WriteLine("UpdateApplication for types: " + PrintTypes(types));
        if (types == null)
            return;

        foreach (var type in types)
        {
            if (!Instances.TryGetValue(type, out var instances))
                continue;

            foreach (var instance in instances)
                instance.Reload();
        }
    }

    public static string PrintTypes(Type[]? types)
    {
        if (types != null)
        {
            return string.Join(", ", types.Select(t => t.Name));
        }

        return "";
    }

    internal static void RegisterInstance(IReloadable instance)
    {
        if (!IsEnabled) return;

        var type = instance.GetType();

        if (type.IsGenericType)
        {
            type = type.GetGenericTypeDefinition();
        }

        if (!Instances.TryGetValue(type, out var instances))
        {
            instances = new HashSet<IReloadable>();
            Instances[type] = instances;
        }

        instances.Add(instance);
    }

    internal static void UnregisterInstance(IReloadable instance)
    {
        if (!IsEnabled) return;

        var type = instance.GetType();

        if (type.IsGenericType)
        {
            type = type.GetGenericTypeDefinition();
        }

        if (!Instances.TryGetValue(type, out var instances)) return;

        if (instances.Contains(instance))
            instances.Remove(instance);
    }
}
</file>

<file path="Avalonia.Markup.Declarative/IComponentControlFactory.cs">
using Avalonia.Controls;
using System.Diagnostics.CodeAnalysis;

namespace Avalonia.Markup.Declarative;

/// <summary>
/// Defines a factory interface for creating Avalonia <see cref="Control"/> instances with support for dependency injection.
/// Implementations can use an <see cref="IServiceProvider"/> or other mechanisms to inject services into control constructors.
///
/// <para>
/// <b>Example implementation using Microsoft.Extensions.DependencyInjection:</b>
/// <code language="csharp">
/// public class ControlFactory(IServiceProvider serviceProvider) : IComponentControlFactory
/// {
///     public TControl CreateControlInstance&lt;TControl&gt;()
///         where TControl : Control
///         => ActivatorUtilities.CreateInstance&lt;TControl&gt;(serviceProvider);
/// }
/// </code>
/// </para>
/// </summary>
public interface IComponentControlFactory
{
    /// <summary>
    /// Creates an instance of the specified <see cref="Control"/> type, optionally injecting dependencies.
    /// </summary>
    /// <typeparam name="TControl">The type of control to create. Must have a public constructor.</typeparam>
    /// <returns>An instance of <typeparamref name="TControl"/>.</returns>
    TControl CreateControlInstance<[DynamicallyAccessedMembers(DynamicallyAccessedMemberTypes.PublicConstructors)] TControl>() where TControl : Control;
}
</file>

<file path="Avalonia.Markup.Declarative/IDeclarativeViewBase.cs">
namespace Avalonia.Markup.Declarative;

/// <summary>
/// Used for triggering Source generator to add Extensions for user controls
/// </summary>
public interface IDeclarativeViewBase
{
}
</file>

<file path="Avalonia.Markup.Declarative/IReloadable.cs">
namespace Avalonia.Markup.Declarative;

/// <summary>
/// IReloadable interfece is used by HotReloadManager to reload controls after net6 updating assemblies on runtime
/// </summary>
public interface IReloadable
{
    /// <summary>
    /// Force control to be reloaded
    /// </summary>
    void Reload();
}
</file>

<file path="Avalonia.Markup.Declarative/README.md">
--------------------------------
Avalonia.Markup.Declarative
--------------------------------

Write Avalonia UI with pure C#

repo: 
https://github.com/AvaloniaUI/Avalonia.Markup.Declarative


to use with external libraries, install AvaloniaExtensionGenerator tool here:

https://www.nuget.org/packages/AvaloniaExtensionGenerator/


Quick Start sample:

https://github.com/AvaloniaUI/Avalonia.Markup.Declarative/tree/master/src/Samples/DeclarativeComponentTemplate
</file>

<file path="Avalonia.Markup.Declarative/readme.txt">
--------------------------------
Avalonia.Markup.Declarative
--------------------------------

Write Avalonia UI with pure C#

repo: 
https://github.com/AvaloniaUI/Avalonia.Markup.Declarative


to use with external libraries, install AvaloniaExtensionGenerator tool here:

https://www.nuget.org/packages/AvaloniaExtensionGenerator/


Quick Start sample:

https://github.com/AvaloniaUI/Avalonia.Markup.Declarative/tree/master/src/Samples/DeclarativeComponentTemplate
</file>

<file path="Avalonia.Markup.Declarative/StylePropertyExtensions.cs">
using Avalonia;
using Avalonia.Controls;
using Avalonia.Data;
using Avalonia.Data.Converters;
using Avalonia.Styling;
using System;
using System.Collections.Generic;
using System.Linq.Expressions;

namespace Avalonia.Markup.Declarative;

public static class StylePropertyExtensions
{
    /// <summary>
    /// Creates selector and applies .OfType<TElement>() to it
    /// </summary>
    /// <typeparam name="TElement">Type of the control that style will be applied</typeparam>
    /// <param name="style">Style</param>
    /// <param name="selector">Selector modifier function</param>
    /// <returns>style with applied selector</returns>
    public static Style<TElement> Selector<TElement>(this Style<TElement> style, Func<Selector, Selector> selector)
        where TElement : StyledElement
    {
        Selector TypeSelector(Selector? s) => s.OfType<TElement>();
        style.Selector = selector(TypeSelector(null));
        return style;
    }

    public static Style Selector(this Style style, Func<Selector?, Selector> selector)
    {
        style.Selector = selector(null);
        return style;
    }

    public static Style<TElement> Setter<TElement>(this Style<TElement> style, AvaloniaProperty avaloniaProperty, object value)
        where TElement : StyledElement
    {
        style._addSetter(avaloniaProperty, value);
        return style;
    }

    public static Style Setter(this Style style, AvaloniaProperty avaloniaProperty, object value)
    {
        style.Setters.Add(new Setter(avaloniaProperty, value));
        return style;
    }

    public static Style<TElement> _addSetter<TElement>(this Style<TElement> style, AvaloniaProperty avaloniaProperty, object value) 
        where TElement : StyledElement
    {
        style.Setters.Add(new Setter(avaloniaProperty, value));
        return style;
    }

    /// <summary>
    /// Adds a style setter using a strongly-typed expression.
    /// The expression is converted to a property path for DataContext-relative binding
    /// (the source parameter is used only for generic type inference).
    /// </summary>
    public static Style<TElement> _addSetterCompiledBinding<TElement, TViewModel, TValue>(
        this Style<TElement> style,
        AvaloniaProperty avaloniaProperty,
        TViewModel source,
        Expression<Func<TViewModel, TValue>> getter,
        BindingMode? bindingMode = null,
        IValueConverter? converter = null)
        where TElement : StyledElement
    {
        var propertyPath = ExpressionToPropertyPath(getter);
        var binding = new Binding(propertyPath)
        {
            Mode = bindingMode ?? BindingMode.Default,
            Converter = converter
        };

        style.Setters.Add(new Setter(avaloniaProperty, binding));
        return style;
    }

    private static string ExpressionToPropertyPath<TIn, TOut>(Expression<Func<TIn, TOut>> expression)
    {
        var members = new List<string>();
        var expr = expression.Body;

        // Unwrap Convert/ConvertChecked (e.g. boxing casts)
        if (expr is UnaryExpression { NodeType: ExpressionType.Convert or ExpressionType.ConvertChecked } unary)
            expr = unary.Operand;

        while (expr is MemberExpression memberExpr)
        {
            members.Insert(0, memberExpr.Member.Name);
            expr = memberExpr.Expression;
        }

        return string.Join(".", members);
    }

    public static Style<TElement> Col<TElement>(this Style<TElement> style, int value)
        where TElement : Control
    {
        style.Add(new Setter(Grid.ColumnProperty, value));
        return style;
    }

    public static Style<TElement> Row<TElement>(this Style<TElement> style, int value)
        where TElement : Control
    {
        style.Add(new Setter(Grid.RowProperty, value));
        return style;
    }

    public static Style<TElement> ColSpan<TElement>(this Style<TElement> style, int value)
        where TElement : Control
    {
        style.Add(new Setter(Grid.ColumnSpanProperty, value));
        return style;
    }

    public static Style<TElement> RowSpan<TElement>(this Style<TElement> style, int value)
        where TElement : Control
    {
        style.Add(new Setter(Grid.RowSpanProperty, value));
        return style;
    }

}
</file>

<file path="Avalonia.Markup.Declarative/Styles/Style.cs">
using Avalonia.Markup.Declarative.Helpers;
using Avalonia.Styling;
using System;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative;
/// <summary>
/// Typed style to support method chains with generic arguments
/// </summary>
/// <typeparam name="TControl">Type of the control that style will be applied to</typeparam>
public class Style<TControl> : Style, IRelativeStyle
    where TControl : StyledElement
{
    private Func<Selector, Selector> SelectorFunc { get; }

    /// <summary>
    /// Creates Style with added .OfType<typeparam name="TControl"></typeparam> selector
    /// </summary>
    public Style()
    {
        SelectorFunc = s => s.OfType<TControl>();
    }

    /// <summary>
    /// Creates Style with added .OfType<typeparam name="TControl"></typeparam> selector and use name="selectorFunc" to generate selector
    /// </summary>
    /// <param name="selectorFunc">Selector to control</param>
    /// <param name="expression">expression how selector was called</param>
    /// <param name="callerFile">file where style was constructed</param>
    public Style(Func<Selector, Selector> selectorFunc, [CallerArgumentExpression(nameof(selectorFunc))] string? expression = null, [CallerFilePath] string? callerFile = null)
    {
        SelectorFunc = selectorFunc;

        //add TypeOf<> Selector as a beginning of the chain if it's not specified in selectorFunc body argument already
        if (selectorFunc(null!).GetTypeNameFromSelector() == null) 
            SelectorFunc = s => selectorFunc(s.OfType<TControl>());
    }

    public void UpdateSelector(Func<Selector, Selector>? baseSelectorFunc)
    {
        if (baseSelectorFunc != null)
            Selector = SelectorFunc(baseSelectorFunc(null!));
        else
            Selector = SelectorFunc(null!);
    }
}

internal interface IRelativeStyle : IStyle
{
    void UpdateSelector(Func<Selector, Selector>? baseSelectorFunc);
}
</file>

<file path="Avalonia.Markup.Declarative/Styles/StyleBuilder.cs">
using System;
using System.Collections.Generic;
using Avalonia.Styling;

namespace Avalonia.Markup.Declarative;

internal static class StyleBuilder
{
    internal static IEnumerable<IStyle> StylesToRange(StyleGroup? styleGroup, Func<Selector, Selector>? selector = null)
    {
        if (styleGroup == null)
            yield break;

        var stack = new Stack<(StyleGroup, Func<Selector, Selector>?)>();
        stack.Push((styleGroup, selector));

        while (stack.Count > 0)
        {
            var (currentGroup, currentSelectorFunc) = stack.Pop();

            foreach (var item in currentGroup)
            {
                switch (item)
                {
                    case IRelativeStyle relStyle:
                        relStyle.UpdateSelector(currentSelectorFunc);
                        yield return relStyle;
                        break;
                    case Style style:
                        if (currentSelectorFunc != null)
                            style.Selector = currentSelectorFunc(style.Selector!);
                        yield return style;
                        break;

                    case IStyle iStyle:
                        yield return iStyle;
                        break;

                    case StyleGroup group:
                        var groupSelectorFunc = currentSelectorFunc;

                        if (group.GroupSelectorFunc != null && currentSelectorFunc != null)
                            groupSelectorFunc = s => group.GroupSelectorFunc(currentSelectorFunc(s));
                        else if (group.GroupSelectorFunc != null)
                            groupSelectorFunc = s => group.GroupSelectorFunc(s);

                        stack.Push((group, groupSelectorFunc));
                        break;
                }
            }
        }
    }
}
</file>

<file path="Avalonia.Markup.Declarative/Styles/StyleGroup.cs">
using System;
using System.Collections.Generic;
using Avalonia.Styling;

namespace Avalonia.Markup.Declarative;

public class StyleGroup(Func<Selector,Selector>? groupSelectorFunc = null) : List<object>
{
    public Func<Selector, Selector>? GroupSelectorFunc { get; } = groupSelectorFunc;

    public override string ToString()
    {
        if (GroupSelectorFunc != null)
        {
            return GroupSelectorFunc(null!).ToString();
        }
        return base.ToString() ?? "- No selector --";
    }
}
</file>

<file path="Avalonia.Markup.Declarative/ViewBase.cs">
using Avalonia.Controls;
using Avalonia.Threading;
using System;
using System.Collections.Immutable;
using System.Diagnostics;

namespace Avalonia.Markup.Declarative;

/// <summary>
/// Defines when a view should be initialized.
/// </summary>
public enum ViewInitializationStrategy
{
    /// <summary>
    /// View is initialized lazily when first accessed (e.g., when Child property is accessed or when attached to visual tree).
    /// </summary>
    Lazy,
    
    /// <summary>
    /// View is initialized immediately in the constructor.
    /// </summary>
    Immediate
}

public abstract class ViewBase<TViewModel> : ViewBase
    where TViewModel : class
{
    public virtual TViewModel? ViewModel
    {
        get => (TViewModel?)DataContext!;
        set => DataContext = value;
    }

    //injecting viewmodel through constructor is optional, but if you do it, the view will be initialized immediately with provided viewmodel as DataContext
    protected ViewBase(TViewModel viewModel) : base(ViewInitializationStrategy.Lazy)
    {
        DataContext = viewModel ?? throw new ArgumentNullException(nameof(viewModel));
        Initialize();
    }

    //classic mvvm approach with parameterless constructor and setting DataContext later is also supported, but in this case the view will be initialized lazily when first accessed or attached to visual tree
    protected ViewBase() : base(ViewInitializationStrategy.Lazy)
    {
        // not calling Initialize()! Waiting for OnDataContextChanged
    }

    protected override void OnDataContextChanged(EventArgs e)
    {
        base.OnDataContextChanged(e);

        // once DataContext is set to a compatible viewmodel type, we can initialize the view if it hasn't been initialized yet
        if (DataContext is TViewModel && !IsInitialized)
        {
            Initialize();
        }
    }

    protected abstract object Build(TViewModel vm);

    protected override object Build()
    {
        if (DataContext is not TViewModel vm)
        {
            throw new InvalidOperationException(
                $"Cannot build view {GetType().Name} without a valid ViewModel of type {typeof(TViewModel).Name}.");
        }

        return Build(vm);
    }
}

/// <summary>
/// Base view class used like UserControl in XAML
/// </summary>
public abstract class ViewBase : Decorator, IReloadable, IDeclarativeViewBase
{
    private INameScope? _nameScope;

    /// <summary>
    /// Current NameScope of this view
    /// </summary>
    protected INameScope Scope => _nameScope ??= new NameScope();

    public event Action? ViewInitialized;

    protected abstract object Build();

    protected virtual StyleGroup? BuildStyles() => null;

    protected ViewBase() 
        : this(AppBuilderExtensions.DefaultViewInitializationStrategy)
    {
    }

    protected ViewBase(ViewInitializationStrategy initializationStrategy)
    {
        InitializationStrategy = initializationStrategy;

        if (initializationStrategy == ViewInitializationStrategy.Immediate)
            Initialize();
    }

    /// <summary>
    /// Gets the initialization strategy used by this view.
    /// </summary>
    public ViewInitializationStrategy InitializationStrategy { get; }

    private bool _isInitialized;
    private bool _isInitializing;

    private void EnsureInitialized()
    {
        if (!_isInitialized && !_isInitializing)
        {
            Initialize();
        }
    }

    /// <summary>
    /// Called from constructor, right before initialization and building UI
    /// Override this method when you want to run some stuff before creation of children controls
    /// </summary>
    protected virtual void OnCreated()
    {
    }

    protected virtual void OnAfterInitialized() 
    {
    }

    public void Initialize()
    {
        if (_isInitialized || _isInitializing)
            return;
            
        _isInitializing = true;
        
        try
        {
            OnCreated();
            NameScope.SetNameScope(this, Scope);

            if (BuildStyles() is { } styleGroup)
            {
                var viewStyles = StyleBuilder.StylesToRange(styleGroup).ToImmutableList();
                Styles.AddRange(viewStyles);
            }

            var content = Build();
            Child = content as Control;

            _isInitialized = true;
            ViewInitialized?.Invoke();
            OnAfterInitialized();
        }
        catch (Exception ex)
        {
            Debug.WriteLine(ex.Message);
            Debug.WriteLine(ex.StackTrace);
            throw new ViewBuildingException(CreateBuildErrorMessage(GetType(), ex), ex);
        }
        finally
        {
            _isInitializing = false;
        }
    }

    private static string CreateBuildErrorMessage(Type viewType, Exception exception)
    {
        var rootCause = GetRootCause(exception);
        var message = $"Build error in {viewType.Name} ({rootCause.GetType().Name}): {rootCause.Message}";

        if (IsBinaryCompatibilityException(rootCause))
        {
            message += " This usually means an external package was built against a different version of Avalonia or another dependency. Check the package's declared dependency versions against the versions resolved by the app.";
        }

        return message;
    }

    private static Exception GetRootCause(Exception exception)
    {
        while (exception.InnerException != null)
        {
            exception = exception.InnerException;
        }

        return exception;
    }

    private static bool IsBinaryCompatibilityException(Exception exception) =>
        exception is MissingFieldException or MissingMethodException or TypeLoadException or System.IO.FileLoadException;

    /// <summary>
    /// Gets the child control, ensuring initialization if needed.
    /// </summary>
    public new Control? Child
    {
        get
        {
            EnsureInitialized();
            return base.Child;
        }
        protected set => base.Child = value;
    }

    #region Hot reload stuff
    public void Reload()
    {
        Dispatcher.UIThread.InvokeAsync(() =>
        {
            OnBeforeReload();
            SetValue(ChildProperty, null);
            VisualChildren.Clear();
            _nameScope = null;
            _isInitialized = false;
            _isInitializing = false;

            Initialize();

            InvalidateArrange();
            InvalidateMeasure();
            InvalidateVisual();
        });
    }

    protected virtual void OnBeforeReload()
    {
    }

    protected override void OnAttachedToVisualTree(VisualTreeAttachmentEventArgs e)
    {
        base.OnAttachedToVisualTree(e);
        HotReloadManager.RegisterInstance(this);
        EnsureInitialized();
    }

    protected override void OnDetachedFromVisualTree(VisualTreeAttachmentEventArgs e)
    {
        base.OnDetachedFromVisualTree(e);
        HotReloadManager.UnregisterInstance(this);
    }
    #endregion
}

public class ViewBuildingException(string message, Exception innerException) : Exception(message, innerException);
</file>

<file path="Avalonia.Markup.Declarative/ViewFactory.cs">
using Avalonia.Controls;

namespace Avalonia.Markup.Declarative;

/// <summary>
/// Factory for creating control instances with optional dependency injection.
/// Replaces the former ComponentBase.New&lt;T&gt;() pattern.
/// </summary>
public static class ViewFactory
{
    /// <summary>
    /// Creates a control instance using the registered <see cref="IComponentControlFactory"/>
    /// or falls back to parameterless constructor.
    /// </summary>
    public static TControl Create<TControl>() where TControl : Control
    {
        var factory = AppBuilderExtensions.ComponentControlFactory;

        if (factory != null)
            return factory.CreateControlInstance<TControl>();

        return System.Activator.CreateInstance<TControl>();
    }
}
</file>

<file path="Avalonia.Markup.Declarative/WindowExtensions.cs">
using Avalonia.Controls;

namespace Avalonia.Markup.Declarative;

public static class NativeMenuExtensions
{
    public static NativeMenu Items(this NativeMenu menu, params NativeMenuItemBase[] items)
    {
        foreach (var item in items)
            menu.Items.Add(item);

        return menu;
    }

    public static NativeMenuItem Items(this NativeMenuItem menu, params NativeMenuItemBase[] items)
    {
        menu.Menu ??= [];
        foreach (var item in items)
            menu.Menu.Items.Add(item);

        return menu;
    }
}
</file>

<file path="Samples/AvaloniaMarkupSample/App.cs">
using Avalonia.Platform;
using AvaloniaMarkupSample;

var lifetime = new ClassicDesktopStyleApplicationLifetime { Args = args, ShutdownMode = ShutdownMode.OnLastWindowClose };

var appBuilder = AppBuilder.Configure<Application>()
    .UsePlatformDetect()
    .AfterSetup(b => b.Instance?.Styles.Add(new FluentTheme()))
    .SetupWithLifetime(lifetime);

var icon = AssetLoader.Open( new Uri($"avares://AvaloniaMarkupSample/avalonia-logo.ico"));

var menu = new NativeMenu().Items(
    new NativeMenuItem()
        .Header("File")
        .Items(
            new NativeMenuItem()
                .Header("Open")
                .OnClick(OnOpenClick),

            new NativeMenuItemSeparator(),

            new NativeMenuItem()
                .Header("Close")
                .OnClick(_ => Environment.Exit(0))
        )
);

appBuilder.Instance?.TrayIcon_Icons(
    [
        new TrayIcon()
            .Icon(new WindowIcon(icon))
            .Menu(menu)
    ]
);

lifetime.MainWindow = new Window()
    .Title("Avalonia markup samples")
    .Content(new MainView())
    .NativeMenu_Menu(menu);

void OnOpenClick(EventArgs e)
{

}

#if DEBUG
lifetime.MainWindow.AttachDevTools();
#endif

lifetime.Start(args);
</file>

<file path="Samples/AvaloniaMarkupSample/AvaloniaMarkupSample.csproj">
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <None Remove="avalonia-logo.ico" />
  </ItemGroup>

  <ItemGroup>
    <AvaloniaResource Include="avalonia-logo.ico" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Assets\teapot.bin" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Desktop" Version="12.0.0" />
    <PackageReference Include="Avalonia.Diagnostics" Version="11.3.13" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.0.0" />
    <PackageReference Include="System.Reactive" Version="6.1.0" />
    <PackageReference Include="Tmds.DBus.Protocol" Version="0.92.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Avalonia.Markup.Declarative\Avalonia.Markup.Declarative.csproj" />
    <ProjectReference Include="..\..\Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="System" />
    <Using Include="Avalonia" />
    <Using Include="Avalonia.Controls" />
    <Using Include="Avalonia.Controls.ApplicationLifetimes" />
    <Using Include="Avalonia.Data" />
    <Using Include="Avalonia.Interactivity" />
    <Using Include="Avalonia.Markup.Declarative" />
    <Using Include="Avalonia.Themes.Fluent" />
    <Using Include="Avalonia.Layout" />
    <Using Include="Avalonia.Media" />
  </ItemGroup>

</Project>
</file>

<file path="Samples/AvaloniaMarkupSample/CommonSamples/ItemControlSampleView.cs">
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace AvaloniaMarkupSample.CommonSamples;

public class ItemControlSampleViewModel : INotifyPropertyChanged
{
    private string _selectedItem = "one";
    public string SelectedItem
    {
        get => _selectedItem;
        set { _selectedItem = value; OnPropertyChanged(); }
    }

    public List<string> Items { get; set; } =
    [
        "one",
        "two",
        "four"
    ];

    public event PropertyChangedEventHandler? PropertyChanged;
    protected void OnPropertyChanged([CallerMemberName] string? name = null) =>
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
}

public class ItemControlSampleView() : ViewBase<ItemControlSampleViewModel>(new ItemControlSampleViewModel())
{
    protected override object Build(ItemControlSampleViewModel? vm) =>
        new StackPanel()
            .Children(
                new ListBox()
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .ItemsSource(vm!.Items)
                    .ItemTemplate<string>(item =>
                        new TextBlock()
                            .Background(Brushes.Beige)
                            .Text(item)
                    )
                    .SelectedItem(vm!, x => x.SelectedItem, BindingMode.TwoWay),

                new TextBlock()
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .Text(vm!, x => x.SelectedItem)
            );
}
</file>

<file path="Samples/AvaloniaMarkupSample/CommonSamples/MyCustomControl.cs">
using AvaloniaMarkupSample;

public class MyCustomControl : ViewBase
{
    #region Background Styled Avalonia Property
    public SolidColorBrush? Background
    {
        get => GetValue(BackgroundProperty);
        set => SetValue(BackgroundProperty, value);
    }

    public static readonly StyledProperty<SolidColorBrush?> BackgroundProperty =
        AvaloniaProperty.Register<MyCustomControl, SolidColorBrush?>
        (
            nameof(Background)
        );
    #endregion Background Styled Avalonia Property

    #region NewValue Styled Avalonia Property
    public string? NewValue
    {
        get => GetValue(NewValueProperty);
        set => SetValue(NewValueProperty, value);
    }

    public static readonly StyledProperty<string?> NewValueProperty =
        AvaloniaProperty.Register<MyCustomControl, string?>
        (
            nameof(NewValue)
        );
    #endregion NewValue Styled Avalonia Property


    #region SavedValue Styled Avalonia Property
    public string? SavedValue
    {
        get => GetValue(SavedValueProperty);
        set => SetValue(SavedValueProperty, value);
    }

    public static readonly StyledProperty<string?> SavedValueProperty =
        AvaloniaProperty.Register<MyCustomControl, string?>
        (
            nameof(SavedValue)
        );
    #endregion SavedValue Styled Avalonia Property


    #region CanSave Direct Avalonia Property
    private bool _canSave = default;

    public static readonly DirectProperty<MyCustomControl, bool> CanSaveProperty =
        AvaloniaProperty.RegisterDirect<MyCustomControl, bool>
        (
            nameof(CanSave),
            o => o.CanSave
        );

    public bool CanSave
    {
        get => _canSave;
        private set => SetAndRaise(CanSaveProperty, ref _canSave, value);
    }

    #endregion CanSave Direct Avalonia Property

    // CanSave is set to true when SavedValue is not the same as NewView
    // false otherwise
    private void SetCanSave(object? _)
    {
        CanSave = SavedValue != NewValue;
    }

    public MyCustomControl()
    {
        this.GetObservable(NewValueProperty).Subscribe(SetCanSave);
        this.GetObservable(SavedValueProperty).Subscribe(SetCanSave);
    }

    public void Save()
    {
        SavedValue = NewValue;
    }

    public void Cancel()
    {
        NewValue = SavedValue;
    }

    protected override object Build()
        => new Grid()
            .Rows("Auto, Auto, *, Auto")
            .Children(
                new StackPanel()
                    .Orientation(Orientation.Horizontal)
                    .VerticalAlignment(VerticalAlignment.Center)
                    .Children(
                        new TextBlock().Text("Enter text:")
                            .VerticalAlignment(VerticalAlignment.Center),
                        new TextBox()
                            .Text(NewValueProperty, this, BindingMode.TwoWay)
                            .MinWidth(150)
                    ),

                new StackPanel().Row(1)
                    .Margin(0, 10)
                    .Orientation(Orientation.Horizontal)
                    .VerticalAlignment(VerticalAlignment.Center)
                    .HorizontalAlignment(HorizontalAlignment.Left)
                    .Children(
                        new TextBlock().Text("Saved text:")
                            .VerticalAlignment(VerticalAlignment.Center),
                        new TextBox()
                            .Text(SavedValueProperty, this)
                            .MinWidth(150)
                    ),
                new StackPanel().Row(3)
                    .Orientation(Orientation.Horizontal)
                    .HorizontalAlignment(HorizontalAlignment.Right)
                    .Children(
                        new Button().Content("Cancel")
                            .Margin(5, 0)
                            .IsEnabled(CanSaveProperty, this)
                            .OnClick(args=> Cancel()),
                        new Button().Content("Save")
                            .Margin(5, 0)
                            .IsEnabled(CanSaveProperty, this)
                            .OnClick(args => Save())
                    )
            );

}
</file>

<file path="Samples/AvaloniaMarkupSample/CommonSamples/MyCustomTemplatedControl.cs">
using Avalonia.Controls.Primitives;

public class MyCustomTemplatedControl : TemplatedControl
{
    #region NewValue Styled Avalonia Property
    public string? NewValue
    {
        get { return GetValue(NewValueProperty); }
        set { SetValue(NewValueProperty, value); }
    }

    public static readonly StyledProperty<string?> NewValueProperty =
        AvaloniaProperty.Register<MyCustomTemplatedControl, string?>
        (
            nameof(NewValue)
        );
    #endregion NewValue Styled Avalonia Property


    #region SavedValue Styled Avalonia Property
    public string? SavedValue
    {
        get { return GetValue(SavedValueProperty); }
        set { SetValue(SavedValueProperty, value); }
    }

    public static readonly StyledProperty<string?> SavedValueProperty =
        AvaloniaProperty.Register<MyCustomTemplatedControl, string?>
        (
            nameof(SavedValue)
        );
    #endregion SavedValue Styled Avalonia Property


    #region CanSave Direct Avalonia Property
    private bool _canSave = default;

    public static readonly DirectProperty<MyCustomTemplatedControl, bool> CanSaveProperty =
        AvaloniaProperty.RegisterDirect<MyCustomTemplatedControl, bool>
        (
            nameof(CanSave),
            o => o.CanSave
        );

    public bool CanSave
    {
        get => _canSave;
        private set
        {
            SetAndRaise(CanSaveProperty, ref _canSave, value);
        }
    }

    #endregion CanSave Direct Avalonia Property

    // CanSave is set to true when SavedValue is not the same as NewView
    // false otherwise
    private void SetCanSave(object? _)
    {
        CanSave = SavedValue != NewValue;
    }

    public MyCustomTemplatedControl()
    {
        this.GetObservable(NewValueProperty).Subscribe(SetCanSave);
        this.GetObservable(SavedValueProperty).Subscribe(SetCanSave);
    }

    public void Save()
    {
        SavedValue = NewValue;
    }

    public void Cancel()
    {
        NewValue = SavedValue;
    }

}
</file>

<file path="Samples/AvaloniaMarkupSample/CommonSamples/StylesSampleView.cs">
using Avalonia.Controls.Templates;
using Avalonia.Styling;

namespace AvaloniaMarkupSample.CommonSamples;

public class StylesSampleView : ViewBase
{
    public TabViewModel TabViewModel { get; } = new("Template");

    protected override StyleGroup? BuildStyles() =>
    [
        new Style<Button>(s => s.Class("nested-button"))
            .FontSize(26d)
    ];

    protected override object Build() =>
        new StackPanel()
            .Classes("sample-wrapper")
            .Children(
            [
                new TabControl()
                    .ItemTemplate<TabViewModel>(tab => new TextBlock().Text(tab.Title))
                    .ContentTemplate(
                        new FuncDataTemplate<TabViewModel>((item, ns) =>
                            new FuncView<TabViewModel>(item, vm =>
                                new TextBlock().Text(vm, x => x.Content)))
                    )
                    .ItemsSource(Tabs)
                    .Styles(
                        new Style<TabItem>()
                            .IsEnabled(default(TabViewModel)!, x => x.Enabled)
                            .Foreground(Brushes.YellowGreen)
                    ),

                new Button()
                    .Classes("nested-button")
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .Content("Hello styles!")
                    .Height(50)
                    .Styles(
                        // Typed generic style
                        new Style<Button>(x =>
                                x.OfType<StackPanel>().Class("sample-wrapper").Descendant().OfType<Button>()
                                    .Class("nested-button"))
                            .Background(Brushes.Green),
                        new Style<Button>(s =>
                                s.OfType<Button>().Class(":pointerover")
                                    .Child()) //add child selector to change color of content presenter inside button
                            .Background(Brushes.Red)),

                new Button()
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .Content("Unstyled buton")
                    .Width(150)
            ]);

    public List<TabViewModel> Tabs { get; set; } = new()
    {
        new TabViewModel("Tab1"),
        new TabViewModel("Tab2"),
        new TabViewModel("Disabled tab",false)
    };
}

public class TabViewModel
{
    public TabViewModel(string title, bool enabled = true)
    {
        Title = title;
        Enabled = enabled;
    }

    public string Title { get; }
    public bool Enabled { get; }

    public string Content => Title + " Content";
}
</file>

<file path="Samples/AvaloniaMarkupSample/CommonSamples/TreeViewSampleView.cs">
using System.Collections.ObjectModel;
using System.ComponentModel;
using System.Runtime.CompilerServices;
using Avalonia.Controls.Templates;

namespace AvaloniaMarkupSample.CommonSamples;

public class TreeViewSampleViewModel : INotifyPropertyChanged
{
    private Node? _selectedNode;
    public Node? SelectedNode
    {
        get => _selectedNode;
        set { _selectedNode = value; OnPropertyChanged(); }
    }

    public ObservableCollection<Node> Nodes { get; set; } =
    [
        new Node("Animals", [
            new Node("Mammals", [
                new Node("Lion"), new Node("Cat"), new Node("Zebra")
            ])
        ])
    ];

    public event PropertyChangedEventHandler? PropertyChanged;
    protected void OnPropertyChanged([CallerMemberName] string? name = null) =>
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
}

public class TreeViewSampleView() : ViewBase<TreeViewSampleViewModel>(new TreeViewSampleViewModel())
{
    protected override object Build(TreeViewSampleViewModel? vm) =>
        new StackPanel()
            .Children(
                new TreeView()
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .ItemsSource(vm!.Nodes)
                    .ItemTemplate(
                        new FuncTreeDataTemplate<Node>(
                            (n, _) => new TextBlock().Text(n.Name),
                            n => n.Children)
                    )
                    .SelectedItem(vm!, x => x.SelectedNode!, BindingMode.TwoWay),

                new TextBlock()
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .Text(vm!, x => x.SelectedNode!.Name)
            );
}

public class Node(string name, IEnumerable<Node>? children = null)
{
    public string Name { get; set; } = name;

    public ObservableCollection<Node> Children { get; set; } = new(children ?? []);
}
</file>

<file path="Samples/AvaloniaMarkupSample/MainView.cs">
using Avalonia.Controls.Primitives;
using Avalonia.Styling;
using AvaloniaMarkupSample.CommonSamples;
using AvaloniaMarkupSample.MvvmSample;

namespace AvaloniaMarkupSample;

public class MainView : ViewBase
{
    protected override StyleGroup? BuildStyles() =>
    [
        new Style<Border>(s => s.OfType<TabItem>().Child())
            .BorderBrush(Brushes.Gray)
            .BorderThickness(1)
    ];

    protected override object Build() =>
        new TabControl()
            .SelectedIndex(0)
            .TabStripPlacement(Dock.Left)
            .Items(
                new TabItem()
                    .Header("Hot reload")
                    .Content(
                        new StackPanel()
                            .Children(
                                new TextBlock()
                                    .Ref(out var textBlock)
                                    .Padding(12)
                                    .FontSize(30)
                                    .HorizontalAlignment(HorizontalAlignment.Center)
                                    .Text("Hello Hot Reload!"),

                                new Button()
                                    .Name("HotReloadButton", Scope)
                                    .Content("click me!")
                                    .HorizontalAlignment(HorizontalAlignment.Center)
                                    .OnClick(_ =>
                                    {
                                        textBlock.Text("Button clicked!");

                                        var button = this.FindControl<Button>("HotReloadButton");
                                        if (button != null)
                                            button.Content = "Found by Name";
                                    }),
                                new HyperlinkButton()
                                    .HorizontalAlignment(HorizontalAlignment.Center)
                                    .NavigateUri(new Uri("https://github.com/AvaloniaUI/Avalonia.Markup.Declarative"))
                                    .Content("Open on github")
                            )
                    ),

                new TabItem()
                    .Header("Dock sample")
                    .Content(
                        new Border().Child(
                            new DockPanel()
                                .LastChildFill(true)
                                .Children(
                                    new TextBox().Text("Left1").Dock(Dock.Left),
                                    new TextBox().Text("Right").Dock(Dock.Right),
                                    new TextBox().Text("Left2").Dock(Dock.Left)))
                    ),

                new TabItem()
                    .Header("MVVM")
                    .Content(new Border().Child(new MvvmSampleView())
                    ),

                new TabItem()
                    .Header("Styles")
                    .Content(new Border().Child(new StylesSampleView())),

                new TabItem()
                    .Header("Treeview control")
                    .Content(new Border().Child(new TreeViewSampleView())),

                new TabItem()
                    .Header("Item control")
                    .Content(new Border().Child(new ItemControlSampleView())),

                new TabItem().Header("Custom controls")
                    .Content(
                        new StackPanel()
                            .Width(300)
                            .Children(
                                new TextBlock()
                                    .Text("Custom control example:"),

                                new Border()
                                    .BorderBrush(Brushes.Gray)
                                    .BorderThickness(1)
                                    .Child(
                                        new MyCustomControl()
                                            .Margin(20)
                                    ),

                                new TextBlock()
                                    .Text("Custom templated control example:"),

                                new Border()
                                    .BorderBrush(Brushes.Gray)
                                    .BorderThickness(1)
                                    .Child(
                                        new MyCustomTemplatedControl()
                                            .Ref(out var myCustomTemplatedControl)
                                            .Margin(20)
                                            .Template(StaticResources.Templates.MyControlTemplate)
                                    ),
                                new ToggleButton()
                                    .Ref(out var tb)
                                    .Content("Change template")
                                    .OnClick(args =>
                                    {
                                        myCustomTemplatedControl.Template = !(tb.IsChecked ?? false)
                                            ? StaticResources.Templates.MyControlTemplate
                                            : StaticResources.Templates.MyAnotherControlTemplate;
                                    })
                            )
                    ));

}
</file>

<file path="Samples/AvaloniaMarkupSample/MvvmSample/MvvmSampleView.cs">
namespace AvaloniaMarkupSample.MvvmSample;

public class MvvmSampleView() : ViewBase<MvvmSampleViewModel>(new MvvmSampleViewModel())
{
    protected override object Build(MvvmSampleViewModel? vm) =>
        new StackPanel()
            .Children(
                new TextBlock()
                    .Text(vm!, x => x.MyObject.MyProperty),

                new Button()
                    .Content("Execute Command")
                    .OnClick(_ => vm?.MyCommand(vm))
            );
}
</file>

<file path="Samples/AvaloniaMarkupSample/MvvmSample/MvvmSampleViewModel.cs">
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace AvaloniaMarkupSample.MvvmSample;

public record struct MyObject(
    string MyProperty);

public class MvvmSampleViewModel : INotifyPropertyChanged
{
    private MyObject _myObject = new("Click the button below.");

    public MyObject MyObject
    {
        get => _myObject;
        set
        {
            if (_myObject != value)
            {
                _myObject = value;
                OnPropertyChanged();
            }
        }
    }

    public void MyCommand(object? commandParameter)
    {
        MyObject = new($"You called command with parameter: {commandParameter}");
    }

    public event PropertyChangedEventHandler? PropertyChanged;

    protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
    {
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
    }
}
</file>

<file path="Samples/AvaloniaMarkupSample/Properties/launchSettings.json">
{
  "profiles": {
    "AvaloniaMarkupSample": {
      "commandName": "Project"
    },
    "WSL": {
      "commandName": "WSL2",
      "distributionName": ""
    }
  }
}
</file>

<file path="Samples/AvaloniaMarkupSample/StaticResources.cs">
using Avalonia.Controls.Templates;

public static class StaticResources
{
    public static class Templates
    {
        public static IControlTemplate MyControlTemplate { get; } = new FuncControlTemplate<MyCustomTemplatedControl>(
            (control, scope) => new FuncView<MyCustomTemplatedControl>(control, c =>
                new Grid()
                    .Rows("Auto, Auto, *, Auto")
                    .Children(
                        new StackPanel()
                            .Orientation(Orientation.Horizontal)
                            .VerticalAlignment(VerticalAlignment.Center)
                            .Children(
                                new TextBlock().Text("Enter text:")
                                    .VerticalAlignment(VerticalAlignment.Center),
                                new TextBox()
                                    .Text(MyCustomTemplatedControl.NewValueProperty, c, BindingMode.TwoWay)
                                    .MinWidth(150)
                            ),
                        new StackPanel().Row(1)
                            .Margin(0, 10)
                            .Orientation(Orientation.Horizontal)
                            .VerticalAlignment(VerticalAlignment.Center)
                            .HorizontalAlignment(HorizontalAlignment.Left)
                            .Children(
                                new TextBlock().Text("Saved text:")
                                    .VerticalAlignment(VerticalAlignment.Center),
                                new TextBox()
                                    .Text(MyCustomTemplatedControl.SavedValueProperty, c)
                                    .MinWidth(150)
                            ),
                        new StackPanel().Row(3)
                            .Orientation(Orientation.Horizontal)
                            .HorizontalAlignment(HorizontalAlignment.Right)
                            .Children(
                                new Button().Content("Cancel")
                                    .Margin(5, 0)
                                    .IsEnabled(MyCustomTemplatedControl.CanSaveProperty, c)
                                    .OnClick(_ => c.Cancel()),
                                new Button().Content("Save")
                                    .Margin(5, 0)
                                    .IsEnabled(MyCustomTemplatedControl.CanSaveProperty, c)
                                    .OnClick(_ => c.Save())
                            )
                    ))
            );

        public static IControlTemplate MyAnotherControlTemplate { get; } =
            new FuncControlTemplate<MyCustomTemplatedControl>((control, scope) =>
                new FuncView<MyCustomTemplatedControl>(control, c =>
                    new StackPanel()
                        .Children(
                            new StackPanel()
                                .Orientation(Orientation.Horizontal)
                                .VerticalAlignment(VerticalAlignment.Center)
                                .Children(
                                    new TextBlock()
                                        .Text(c, x => x.NewValue!)
                                        .MinWidth(150)
                                ),
                            new StackPanel()
                                .Margin(0, 10)
                                .Orientation(Orientation.Horizontal)
                                .VerticalAlignment(VerticalAlignment.Center)
                                .HorizontalAlignment(HorizontalAlignment.Left)
                                .Children(
                                    new TextBlock()
                                        .Text(c, x => x.SavedValue!)
                                        .MinWidth(150)
                                )
                        ))
            );
    }
}
</file>

<file path="Samples/DeclarativeComponentTemplate/.gitignore">
## Ignore Visual Studio temporary files, build results, and
## files generated by popular Visual Studio add-ons.
##
## Get latest from https://github.com/github/gitignore/blob/master/VisualStudio.gitignore

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# User-specific files (MonoDevelop/Xamarin Studio)
*.userprefs

# Mono auto generated files
mono_crash.*

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Visual Studio 2015/2017 cache/options directory
.vs/
# Uncomment if you have tasks that create the project's static files in wwwroot
#wwwroot/

# Visual Studio 2017 auto generated files
Generated\ Files/

# MSTest test Results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

# Build Results of an ATL Project
[Dd]ebugPS/
[Rr]eleasePS/
dlldata.c

# Benchmark Results
BenchmarkDotNet.Artifacts/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# Tye
.tye/

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# StyleCop
StyleCopReport.xml

# Files built by Visual Studio
*_i.c
*_p.c
*_h.h
*.ilk
*.meta
*.obj
*.iobj
*.pch
*.pdb
*.ipdb
*.pgc
*.pgd
*.rsp
*.sbr
*.tlb
*.tli
*.tlh
*.tmp
*.tmp_proj
*_wpftmp.csproj
*.log
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc

# Chutzpah Test files
_Chutzpah*

# Visual C++ cache files
ipch/
*.aps
*.ncb
*.opendb
*.opensdf
*.sdf
*.cachefile
*.VC.db
*.VC.VC.opendb

# Visual Studio profiler
*.psess
*.vsp
*.vspx
*.sap

# Visual Studio Trace Files
*.e2e

# TFS 2012 Local Workspace
$tf/

# Guidance Automation Toolkit
*.gpState

# ReSharper is a .NET coding add-in
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# TeamCity is a build add-in
_TeamCity*

# DotCover is a Code Coverage Tool
*.dotCover

# AxoCover is a Code Coverage Tool
.axoCover/*
!.axoCover/settings.json

# Coverlet is a free, cross platform Code Coverage Tool
coverage*.json
coverage*.xml
coverage*.info

# Visual Studio code coverage results
*.coverage
*.coveragexml

# NCrunch
_NCrunch_*
.*crunch*.local.xml
nCrunchTemp_*

# MightyMoose
*.mm.*
AutoTest.Net/

# Web workbench (sass)
.sass-cache/

# Installshield output folder
[Ee]xpress/

# DocProject is a documentation generator add-in
DocProject/buildhelp/
DocProject/Help/*.HxT
DocProject/Help/*.HxC
DocProject/Help/*.hhc
DocProject/Help/*.hhk
DocProject/Help/*.hhp
DocProject/Help/Html2
DocProject/Help/html

# Click-Once directory
publish/

# Publish Web Output
*.[Pp]ublish.xml
*.azurePubxml
# Note: Comment the next line if you want to checkin your web deploy settings,
# but database connection strings (with potential passwords) will be unencrypted
*.pubxml
*.publishproj

# Microsoft Azure Web App publish settings. Comment the next line if you want to
# checkin your Azure Web App publish settings, but sensitive information contained
# in these scripts will be unencrypted
PublishScripts/

# NuGet Packages
*.nupkg
# NuGet Symbol Packages
*.snupkg
# The packages folder can be ignored because of Package Restore
**/[Pp]ackages/*
# except build/, which is used as an MSBuild target.
!**/[Pp]ackages/build/
# Uncomment if necessary however generally it will be regenerated when needed
#!**/[Pp]ackages/repositories.config
# NuGet v3's project.json files produces more ignorable files
*.nuget.props
*.nuget.targets

# Microsoft Azure Build Output
csx/
*.build.csdef

# Microsoft Azure Emulator
ecf/
rcf/

# Windows Store app package directories and files
AppPackages/
BundleArtifacts/
Package.StoreAssociation.xml
_pkginfo.txt
*.appx
*.appxbundle
*.appxupload

# Visual Studio cache files
# files ending in .cache can be ignored
*.[Cc]ache
# but keep track of directories ending in .cache
!?*.[Cc]ache/

# Others
ClientBin/
~$*
*~
*.dbmdl
*.dbproj.schemaview
*.jfm
*.pfx
*.publishsettings
orleans.codegen.cs

# Including strong name files can present a security risk
# (https://github.com/github/gitignore/pull/2483#issue-259490424)
#*.snk

# Since there are multiple workflows, uncomment next line to ignore bower_components
# (https://github.com/github/gitignore/pull/1529#issuecomment-104372622)
#bower_components/

# RIA/Silverlight projects
Generated_Code/

# Backup & report files from converting an old project file
# to a newer Visual Studio version. Backup files are not needed,
# because we have git ;-)
_UpgradeReport_Files/
Backup*/
UpgradeLog*.XML
UpgradeLog*.htm
ServiceFabricBackup/
*.rptproj.bak

# SQL Server files
*.mdf
*.ldf
*.ndf

# Business Intelligence projects
*.rdl.data
*.bim.layout
*.bim_*.settings
*.rptproj.rsuser
*- [Bb]ackup.rdl
*- [Bb]ackup ([0-9]).rdl
*- [Bb]ackup ([0-9][0-9]).rdl

# Microsoft Fakes
FakesAssemblies/

# GhostDoc plugin setting file
*.GhostDoc.xml

# Node.js Tools for Visual Studio
.ntvs_analysis.dat
node_modules/

# Visual Studio 6 build log
*.plg

# Visual Studio 6 workspace options file
*.opt

# Visual Studio 6 auto-generated workspace file (contains which files were open etc.)
*.vbw

# Visual Studio LightSwitch build output
**/*.HTMLClient/GeneratedArtifacts
**/*.DesktopClient/GeneratedArtifacts
**/*.DesktopClient/ModelManifest.xml
**/*.Server/GeneratedArtifacts
**/*.Server/ModelManifest.xml
_Pvt_Extensions

# Paket dependency manager
.paket/paket.exe
paket-files/

# FAKE - F# Make
.fake/

# CodeRush personal settings
.cr/personal

# Python Tools for Visual Studio (PTVS)
__pycache__/
*.pyc

# Cake - Uncomment if you are using it
# tools/**
# !tools/packages.config

# Tabs Studio
*.tss

# Telerik's JustMock configuration file
*.jmconfig

# BizTalk build output
*.btp.cs
*.btm.cs
*.odx.cs
*.xsd.cs

# OpenCover UI analysis results
OpenCover/

# Azure Stream Analytics local run output
ASALocalRun/

# MSBuild Binary and Structured Log
*.binlog

# NVidia Nsight GPU debugger configuration file
*.nvuser

# MFractors (Xamarin productivity tool) working folder
.mfractor/

# Local History for Visual Studio
.localhistory/

# BeatPulse healthcheck temp database
healthchecksdb

# Backup folder for Package Reference Convert tool in Visual Studio 2017
MigrationBackup/

# Ionide (cross platform F# VS Code tools) working folder
.ionide/

# Fody - auto-generated XML schema
FodyWeavers.xsd

##
## Visual studio for Mac
##


# globs
Makefile.in
*.userprefs
*.usertasks
config.make
config.status
aclocal.m4
install-sh
autom4te.cache/
*.tar.gz
tarballs/
test-results/

# Mac bundle stuff
*.dmg
*.app

# content below from: https://github.com/github/gitignore/blob/master/Global/macOS.gitignore
# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon


# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# content below from: https://github.com/github/gitignore/blob/master/Global/Windows.gitignore
# Windows thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Dump file
*.stackdump

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# Windows shortcuts
*.lnk

# JetBrains Rider
.idea/
*.sln.iml

##
## Visual Studio Code
##
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
</file>

<file path="Samples/DeclarativeComponentTemplate/App.cs">
using DeclarativeComponentTemplate;
using Microsoft.Extensions.DependencyInjection;

var services = new ServiceCollection();
services.AddSingleton<SampleDataService>();

var lifetime = new ClassicDesktopStyleApplicationLifetime { Args = args, ShutdownMode = ShutdownMode.OnLastWindowClose };

var serviceProvider = services.BuildServiceProvider();

AppBuilder.Configure<Application>()
    .UsePlatformDetect()
    .AfterSetup(b => b.Instance?.Styles.Add(new FluentTheme()))
    .UseServiceProvider(serviceProvider)
    .UseComponentControlFactory(type => (Control)ActivatorUtilities.CreateInstance(serviceProvider, type))
    .UseViewInitializationStrategy(ViewInitializationStrategy.Lazy)
    .SetupWithLifetime(lifetime);

lifetime.MainWindow = new Window()
    .Title("Avalonia Declarative Component Template")
    .Width(800)
    .Height(600)
    .Content(ViewFactory.Create<MainView>());

#if DEBUG
lifetime.MainWindow.AttachDevTools();
#endif

lifetime.Start(args);

public class SampleDataService
{
    public string GetData() => "this text is from sample data service";
}
</file>

<file path="Samples/DeclarativeComponentTemplate/CounterComponent.cs">
using Avalonia.Styling;
using CommunityToolkit.Mvvm.ComponentModel;

namespace DeclarativeComponentTemplate;

public partial class CounterComponent(SampleDataService dataService) : ViewBase<CounterComponent.State>(new State(dataService))
{
    protected override StyleGroup? BuildStyles() =>
    [
        new Style<Button>(x => x.Class(":pointerover").Descendant())
            .Background(Brushes.LightBlue),

        new Style<Button>()
            .Margin(6)
            .Background(Brushes.DarkSalmon),

        new Style<TextBlock>(s => s.OfType<StackPanel>().Name("SideBar").Descendant())
            .FontSize(16)
            .Foreground(Brushes.White),

        new StyleGroup(x => x.Class("narrow").Descendant())
        {
            new Style<StackPanel>(s => s.Name("SideBar"))
                .IsVisible(false)
        }
    ];

    protected override object Build(State state) =>
        new Grid().Cols("150, *")
            .Children(
                new StackPanel()
                    .Name("SideBar")
                    .Background(Brushes.CadetBlue)
                    .Children(
                        new TextBlock()
                            .Name("title")
                            .Margin(new Thickness(left: 16, top: 16, right: 0, bottom: 0))
                            .Text("Sidebar")
                    ),

                new StackPanel().Col(1)
                    .VerticalAlignment(VerticalAlignment.Center)
                    .HorizontalAlignment(HorizontalAlignment.Center)
                    .Children(
                        new TextBlock()
                            .Text(state, x => x.StatusText),
                        new TextBlock()
                            .Text(state, x => x.CounterDisplay),
                        new NumericUpDown()
                            .Value(state, x => x.Counter, BindingMode.TwoWay),
                        new Button()
                            .HorizontalAlignment(HorizontalAlignment.Center)
                            .Content("Click me")
                            .OnClick(_ => state.RefreshStatusText())
                    )
            );

    public partial class State(SampleDataService dataService) : ObservableObject
    {
        [ObservableProperty]
        [NotifyPropertyChangedFor(nameof(CounterDisplay))]
        public partial decimal? Counter { get; set; } = 0;

        [ObservableProperty]
        public partial string StatusText { get; set; } = "Hello world";

        public string CounterDisplay => Counter is null || Counter == 0
            ? "Counter: zero"
            : $"Counter: {Counter}";

        public void RefreshStatusText()
        {
            StatusText = dataService.GetData();
        }
    }
}
</file>

<file path="Samples/DeclarativeComponentTemplate/DeclarativeComponentTemplate.csproj">
<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<!--Avalonia doesen't support TrimMode=link currently,but we are working on that https://github.com/AvaloniaUI/Avalonia/issues/6892 -->
		<TrimMode Condition="'$(Configuration)' != 'Debug'">copyused</TrimMode>
		<BuiltInComInteropSupport>true</BuiltInComInteropSupport>
	</PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)' != 'Debug'">
    <PublishAot>true</PublishAot>
  </PropertyGroup>
	<ItemGroup>
		<None Remove=".gitignore" />
	</ItemGroup>
	<ItemGroup>
		<!--This helps with theme dll-s trimming.
	If you will publish your application in self-contained mode with p:PublishTrimmed=true and it will use Fluent theme Default theme will be trimmed from the output and vice versa.
	https://github.com/AvaloniaUI/Avalonia/issues/5593 -->
		<TrimmableAssembly Include="Avalonia.Themes.Fluent" />
		<TrimmableAssembly Include="Avalonia.Themes.Default" />
	</ItemGroup>
	<ItemGroup>
		<PackageReference Include="Avalonia.Desktop" Version="12.0.0" />
		<PackageReference Include="Avalonia.Themes.Fluent" Version="12.0.0" />
		<!--Condition below is needed to remove Avalonia.Diagnostics package from build output in Release configuration.-->
		<PackageReference Condition="'$(Configuration)' == 'Debug'" Include="Avalonia.Diagnostics" Version="11.3.13" />
		<PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
		<PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.5" />
		<PackageReference Include="Tmds.DBus.Protocol" Version="0.92.0" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\..\Avalonia.Markup.Declarative\Avalonia.Markup.Declarative.csproj" />
		<ProjectReference Include="..\..\Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
	</ItemGroup>

	<ItemGroup>
		<Using Include="System" />
		<Using Include="Avalonia" />
		<Using Include="Avalonia.Controls" />
		<Using Include="Avalonia.Controls.ApplicationLifetimes" />
		<Using Include="Avalonia.Data" />
		<Using Include="Avalonia.Interactivity" />
		<Using Include="Avalonia.Markup.Declarative" />
		<Using Include="Avalonia.Themes.Fluent" />
		<Using Include="Avalonia.Layout" />
		<Using Include="Avalonia.Media" />
	</ItemGroup>
</Project>
</file>

<file path="Samples/DeclarativeComponentTemplate/MainView.cs">
namespace DeclarativeComponentTemplate;

public class MainView() : ViewBase
{
    protected override object Build()
        => ViewFactory.Create<CounterComponent>()
            .Name("MainView", Scope);
}
</file>

<file path="Samples/ReactiveSample/App.cs">
using ReactiveSample.ViewModels;
using ReactiveSample.Views;
using ReactiveUI.Avalonia;

[assembly: GenerateMarkupExtensionsForAssembly(typeof(RoutedViewHost))] //external assembly extensions source generator support

var lifetime = new ClassicDesktopStyleApplicationLifetime
{
    Args = args,
    ShutdownMode = ShutdownMode.OnLastWindowClose
};

var appBuilder = AppBuilder.Configure<Application>()
    .AfterSetup(b => b.Instance?.Styles.Add(new FluentTheme()))
    .UsePlatformDetect()
    .UseReactiveUI(_ => { })
    .SetupWithLifetime(lifetime);

lifetime.MainWindow = new Window()
                            .Content(
                                new MainView()
                                    .DataContext(new MainViewModel())
                            );

#if DEBUG
lifetime.MainWindow.AttachDevTools();
#endif

lifetime.Start(args);
</file>

<file path="Samples/ReactiveSample/AppViewLocator.cs">
using ReactiveSample.ViewModels;
using ReactiveSample.Views;
using ReactiveUI;

namespace ReactiveSample;

public class AppViewLocator : IViewLocator
{
    public IViewFor<TViewModel>? ResolveView<TViewModel>(string? contract = null) where TViewModel : class
    {
        throw new NotImplementedException();
    }

    public IViewFor? ResolveView(object? instance, string? contract = null) => instance switch
    {
        PageViewModel1 context => new PageView1 { DataContext = context },
        PageViewModel2 context => new PageView2 { DataContext = context },
        _ => throw new ArgumentOutOfRangeException(nameof(instance))
    };
}
</file>

<file path="Samples/ReactiveSample/ControlExtensions.Generated/ColorPaletteResources_MarkupExtensions.g.cs">
#nullable enable
using Avalonia.Data;
using Avalonia.Data.Converters;
using System;
using System.Linq.Expressions;
using System.Numerics;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative;
[global::System.CodeDom.Compiler.GeneratedCode("AvaloniaExtensionGenerator", "1.0.0.0")]
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public static partial class ColorPaletteResources_MarkupExtensions
{
//================= Properties ======================//
 // Accent

/*ValueSetterGenerator*/
public static T Accent<T>(this T control, Avalonia.Media.Color value) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
=> control._set(() => control.Accent = value!);

/*BindFromExpressionSetterGenerator*/
public static T Accent<T>(this T control, Func<Avalonia.Media.Color> func, Action<Avalonia.Media.Color>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
   => control._set(Avalonia.Themes.Fluent.ColorPaletteResources.AccentProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T Accent<T>(this T control,Avalonia.Media.Color value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
=> control._setEx(Avalonia.Themes.Fluent.ColorPaletteResources.AccentProperty, ps, () => control.Accent = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T Accent<T>(this T control, IBinding binding) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
   => control._set(Avalonia.Themes.Fluent.ColorPaletteResources.AccentProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T Accent<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
   => control._set(Avalonia.Themes.Fluent.ColorPaletteResources.AccentProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T Accent<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, Avalonia.Media.Color> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.Themes.Fluent.ColorPaletteResources 
=> control._setEx(Avalonia.Themes.Fluent.ColorPaletteResources.AccentProperty, ps, () => control.Accent = converter.TryConvert(value)!, bindingMode, converter, bindingSource);



}
</file>

<file path="Samples/ReactiveSample/ControlExtensions.Generated/FluentTheme_MarkupExtensions.g.cs">
#nullable enable
using Avalonia.Data;
using Avalonia.Data.Converters;
using System;
using System.Linq.Expressions;
using System.Numerics;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative;
[global::System.CodeDom.Compiler.GeneratedCode("AvaloniaExtensionGenerator", "1.0.0.0")]
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public static partial class FluentTheme_MarkupExtensions
{
//================= Properties ======================//
 // DensityStyle

/*ValueSetterGenerator*/
public static T DensityStyle<T>(this T control, Avalonia.Themes.Fluent.DensityStyle value) where T : Avalonia.Themes.Fluent.FluentTheme 
=> control._set(() => control.DensityStyle = value!);

/*BindFromExpressionSetterGenerator*/
public static T DensityStyle<T>(this T control, Func<Avalonia.Themes.Fluent.DensityStyle> func, Action<Avalonia.Themes.Fluent.DensityStyle>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.Themes.Fluent.FluentTheme 
   => control._set(Avalonia.Themes.Fluent.FluentTheme.DensityStyleProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T DensityStyle<T>(this T control,Avalonia.Themes.Fluent.DensityStyle value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.Themes.Fluent.FluentTheme 
=> control._setEx(Avalonia.Themes.Fluent.FluentTheme.DensityStyleProperty, ps, () => control.DensityStyle = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T DensityStyle<T>(this T control, IBinding binding) where T : Avalonia.Themes.Fluent.FluentTheme 
   => control._set(Avalonia.Themes.Fluent.FluentTheme.DensityStyleProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T DensityStyle<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.Themes.Fluent.FluentTheme 
   => control._set(Avalonia.Themes.Fluent.FluentTheme.DensityStyleProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T DensityStyle<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, Avalonia.Themes.Fluent.DensityStyle> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.Themes.Fluent.FluentTheme 
=> control._setEx(Avalonia.Themes.Fluent.FluentTheme.DensityStyleProperty, ps, () => control.DensityStyle = converter.TryConvert(value)!, bindingMode, converter, bindingSource);



}
</file>

<file path="Samples/ReactiveSample/ControlExtensions.Generated/RoutedViewHost_MarkupExtensions.g.cs">
#nullable enable
using Avalonia.Data;
using Avalonia.Data.Converters;
using ReactiveSample;
using ReactiveUI;
using System;
using System.Linq.Expressions;
using System.Numerics;
using System.Runtime.CompilerServices;


namespace Avalonia.Markup.Declarative;
[global::System.CodeDom.Compiler.GeneratedCode("AvaloniaExtensionGenerator", "1.0.0.0")]
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public static partial class RoutedViewHost_MarkupExtensions
{
//================= Properties ======================//
 // Router

/*ValueSetterGenerator*/
public static T Router<T>(this T control, RoutingState value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._set(() => control.Router = value!);

/*BindFromExpressionSetterGenerator*/
public static T Router<T>(this T control, Func<RoutingState> func, Action<RoutingState>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T Router<T>(this T control, RoutingState value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty, ps, () => control.Router = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T Router<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T Router<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T Router<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, RoutingState> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty, ps, () => control.Router = converter.TryConvert(value)!, bindingMode, converter, bindingSource);


 // ViewContract

/*ValueSetterGenerator*/
public static T ViewContract<T>(this T control, System.String value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._set(() => control.ViewContract = value!);

/*BindFromExpressionSetterGenerator*/
public static T ViewContract<T>(this T control, Func<System.String> func, Action<System.String>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T ViewContract<T>(this T control,System.String value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty, ps, () => control.ViewContract = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T ViewContract<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T ViewContract<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T ViewContract<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, System.String> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty, ps, () => control.ViewContract = converter.TryConvert(value)!, bindingMode, converter, bindingSource);


 // DefaultContent

/*ValueSetterGenerator*/
public static T DefaultContent<T>(this T control, System.Object value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._set(() => control.DefaultContent = value!);

/*BindFromExpressionSetterGenerator*/
public static T DefaultContent<T>(this T control, Func<System.Object> func, Action<System.Object>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T DefaultContent<T>(this T control,System.Object value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty, ps, () => control.DefaultContent = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T DefaultContent<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T DefaultContent<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.RoutedViewHost
   => control._set(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T DefaultContent<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, System.Object> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.RoutedViewHost
=> control._setEx(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty, ps, () => control.DefaultContent = converter.TryConvert(value)!, bindingMode, converter, bindingSource);



//================= Styles ======================//
 // Router

/*ValueStyleSetterGenerator*/
public static Style<T> Router<T>(this Style<T> style, RoutingState value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty!, value!);

/*BindingStyleSetterGenerator*/
public static Style<T> Router<T>(this Style<T> style, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.RouterProperty, binding);


 // ViewContract

/*ValueStyleSetterGenerator*/
public static Style<T> ViewContract<T>(this Style<T> style, System.String value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty!, value!);

/*BindingStyleSetterGenerator*/
public static Style<T> ViewContract<T>(this Style<T> style, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.ViewContractProperty, binding);


 // DefaultContent

/*ValueStyleSetterGenerator*/
public static Style<T> DefaultContent<T>(this Style<T> style, System.Object value) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty!, value!);

/*BindingStyleSetterGenerator*/
public static Style<T> DefaultContent<T>(this Style<T> style, IBinding binding) where T : Avalonia.ReactiveUI.RoutedViewHost
=> style._addSetter(Avalonia.ReactiveUI.RoutedViewHost.DefaultContentProperty, binding);



}
</file>

<file path="Samples/ReactiveSample/ControlExtensions.Generated/ViewModelViewHost_MarkupExtensions.g.cs">
#nullable enable
using Avalonia.Data;
using Avalonia.Data.Converters;
using System;
using System.Linq.Expressions;
using System.Numerics;
using System.Runtime.CompilerServices;

namespace Avalonia.Markup.Declarative;
[global::System.CodeDom.Compiler.GeneratedCode("AvaloniaExtensionGenerator", "1.0.0.0")]
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public static partial class ViewModelViewHost_MarkupExtensions
{
//================= Properties ======================//
 // ViewModel

/*ValueSetterGenerator*/
public static T ViewModel<T>(this T control, System.Object value) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._set(() => control.ViewModel = value!);

/*BindFromExpressionSetterGenerator*/
public static T ViewModel<T>(this T control, Func<System.Object> func, Action<System.Object>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewModelProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T ViewModel<T>(this T control,System.Object value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.ViewModelProperty, ps, () => control.ViewModel = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T ViewModel<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewModelProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T ViewModel<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewModelProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T ViewModel<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, System.Object> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.ViewModelProperty, ps, () => control.ViewModel = converter.TryConvert(value)!, bindingMode, converter, bindingSource);


 // ViewContract

/*ValueSetterGenerator*/
public static T ViewContract<T>(this T control, System.String value) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._set(() => control.ViewContract = value!);

/*BindFromExpressionSetterGenerator*/
public static T ViewContract<T>(this T control, Func<System.String> func, Action<System.String>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T ViewContract<T>(this T control,System.String value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty, ps, () => control.ViewContract = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T ViewContract<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T ViewContract<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T ViewContract<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, System.String> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty, ps, () => control.ViewContract = converter.TryConvert(value)!, bindingMode, converter, bindingSource);


 // DefaultContent

/*ValueSetterGenerator*/
public static T DefaultContent<T>(this T control, System.Object value) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._set(() => control.DefaultContent = value!);

/*BindFromExpressionSetterGenerator*/
public static T DefaultContent<T>(this T control, Func<System.Object> func, Action<System.Object>? onChanged = null, [CallerArgumentExpression(nameof(func))] string? expression = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty!, func, onChanged, expression);

/*MagicalSetterGenerator*/
[Obsolete]
public static T DefaultContent<T>(this T control,System.Object value, BindingMode? bindingMode = null, IValueConverter? converter = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty, ps, () => control.DefaultContent = value!, bindingMode, converter, bindingSource);

/*BindSetterGenerator*/
public static T DefaultContent<T>(this T control, IBinding binding) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty, binding);

/*AvaloniaPropertyBindSetterGenerator*/
public static T DefaultContent<T>(this T control, AvaloniaProperty avaloniaProperty, BindingMode? bindingMode = null, IValueConverter? converter = null, ViewBase? overrideView = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
   => control._set(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty, avaloniaProperty, bindingMode, converter, overrideView);

/*MagicalSetterWithConverterGenerator*/
[Obsolete]
public static T DefaultContent<TValue,T>(this T control, TValue value, FuncValueConverter<TValue, System.Object> converter, BindingMode? bindingMode = null, object? bindingSource = null, [CallerArgumentExpression(nameof(value))] string? ps = null) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> control._setEx(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty, ps, () => control.DefaultContent = converter.TryConvert(value)!, bindingMode, converter, bindingSource);



//================= Styles ======================//
 // ViewContract

/*ValueStyleSetterGenerator*/
public static Style<T> ViewContract<T>(this Style<T> style, System.String value) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> style._addSetter(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty!, value!);

/*BindingStyleSetterGenerator*/
public static Style<T> ViewContract<T>(this Style<T> style, IBinding binding) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> style._addSetter(Avalonia.ReactiveUI.ViewModelViewHost.ViewContractProperty, binding);


 // DefaultContent

/*ValueStyleSetterGenerator*/
public static Style<T> DefaultContent<T>(this Style<T> style, System.Object value) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> style._addSetter(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty!, value!);

/*BindingStyleSetterGenerator*/
public static Style<T> DefaultContent<T>(this Style<T> style, IBinding binding) where T : Avalonia.ReactiveUI.ViewModelViewHost 
=> style._addSetter(Avalonia.ReactiveUI.ViewModelViewHost.DefaultContentProperty, binding);



}
</file>

<file path="Samples/ReactiveSample/Properties/launchSettings.json">
{
  "profiles": {
    "ReactiveSample": {
      "commandName": "Project"
    },
    "WSL": {
      "commandName": "WSL2",
      "distributionName": ""
    }
  }
}
</file>

<file path="Samples/ReactiveSample/ReactiveExtensions.cs">
using ReactiveUI;
using System.Linq.Expressions;
using System.Reactive;

namespace ReactiveSample;

public static class ReactiveExtensions
{
    /// <summary>
    /// Binds a control property to a view model property using ReactiveUI's reactive bindings.
    /// </summary>
    /// <typeparam name="TControl">The type of the control (must inherit from AvaloniaObject).</typeparam>
    /// <typeparam name="TViewModel">The type of the view model (must inherit from ReactiveObject).</typeparam>
    /// <typeparam name="TValue">The type of the control property.</typeparam>
    /// <typeparam name="TValue2">The type of the view model property.</typeparam>
    /// <param name="control">The control whose property will be bound.</param>
    /// <param name="prop">The Avalonia property on the control to bind.</param>
    /// <param name="model">The view model instance to bind to.</param>
    /// <param name="propertySelector">An expression selecting the property on the view model to bind.</param>
    /// <param name="onChange">Optional: An action to invoke when the control property changes (for two-way binding).</param>
    /// <param name="valueSelector">Optional: A function to convert the view model property value to the control property value.</param>
    /// <returns>The control instance, for chaining.</returns>
    public static TControl ReactiveBinding<TControl, TViewModel, TValue, TValue2>(this TControl control,
        AvaloniaProperty<TValue?> prop,
        TViewModel model,
        Expression<Func<TViewModel, TValue2?>> propertySelector,
        Action<TValue?>? onChange,
        Func<TValue2?, TValue?>? valueSelector = null) where TControl : AvaloniaObject where TViewModel : ReactiveObject
    {
        //One Way
        model.WhenAnyValue(propertySelector)
             .SubscribeSafe(new AnonymousObserver<TValue2?>(v =>
            {
                control.SetValue(prop, valueSelector == null ? v : valueSelector(v));
            }));

        //Two Way
        if (onChange != null)
        {
            control.GetObservable(prop).SubscribeSafe(new AnonymousObserver<TValue?>(onChange.Invoke));
        }

        return control;
    }

    /// <summary>
    /// Binds a control property to a view model property using ReactiveUI's reactive bindings.
    /// </summary>
    /// <typeparam name="TControl">The type of the control (must inherit from Control).</typeparam>
    /// <typeparam name="TViewModel">The type of the view model (must inherit from ReactiveObject).</typeparam>
    /// <typeparam name="TValue">The type of the property on both the control and the view model.</typeparam>
    /// <param name="control">The control whose property will be bound.</param>
    /// <param name="prop">The Avalonia property on the control to bind.</param>
    /// <param name="model">The view model instance to bind to.</param>
    /// <param name="propertySelector">An expression selecting the property on the view model to bind.</param>
    /// <param name="onChange">Optional: An action to invoke when the control property changes (for two-way binding).</param>
    /// <returns>The control instance, for chaining.</returns>
    public static TControl ReactiveBinding<TControl, TViewModel, TValue>(this TControl control,
        AvaloniaProperty<TValue?> prop,
        TViewModel model,
        Expression<Func<TViewModel, TValue?>> propertySelector,
        Action<TValue?>? onChange = null) where TControl : Control where TViewModel : ReactiveObject
    {
        ReactiveBinding(control, prop, model, propertySelector, onChange, null);

        return control;
    }
}
</file>

<file path="Samples/ReactiveSample/ReactiveSample.csproj">
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <LangVersion>preview</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <None Remove="avalonia-logo.ico" />
  </ItemGroup>

  <ItemGroup>
    <AvaloniaResource Include="avalonia-logo.ico" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Desktop" Version="12.0.0" />
    <PackageReference Include="Avalonia.Diagnostics" Version="11.3.13" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.0.0" />
    <PackageReference Include="ReactiveUI" Version="23.2.1" />
    <PackageReference Include="ReactiveUI.Avalonia" Version="11.4.12" />
    <PackageReference Include="ReactiveUI.SourceGenerators" Version="2.6.1">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="System.Reactive" Version="6.1.0" />
    <PackageReference Include="Tmds.DBus.Protocol" Version="0.92.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Avalonia.Markup.Declarative\Avalonia.Markup.Declarative.csproj" />
    <ProjectReference Include="..\..\Avalonia.Markup.Declarative.SourceGenerator\Avalonia.Markup.Declarative.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <Compile Remove="ControlExtensions.Generated\**\*.cs" />
    <Using Include="System" />
    <Using Include="Avalonia" />
    <Using Include="Avalonia.Controls" />
    <Using Include="Avalonia.Controls.ApplicationLifetimes" />
    <Using Include="Avalonia.Data" />
    <Using Include="Avalonia.Interactivity" />
    <Using Include="Avalonia.Markup.Declarative" />
    <Using Include="Avalonia.Themes.Fluent" />
    <Using Include="Avalonia.Layout" />
    <Using Include="Avalonia.Media" />
  </ItemGroup>

</Project>
</file>

<file path="Samples/ReactiveSample/ViewModels/MainViewModel.cs">
using ReactiveUI;
using ReactiveUI.SourceGenerators;
using System.Reactive;

namespace ReactiveSample.ViewModels;

public partial class MainViewModel : ReactiveObject, IScreen
{
    [Reactive]
    public partial string MyProperty { get; set; } = "Main View";

    // The Router associated with this Screen.
    // Required by the IScreen interface.
    public RoutingState Router { get; } = new();

    // The command that navigates a user to first view model.
    public ReactiveCommand<Unit, IRoutableViewModel> GoNext { get; }

    // The command that navigates a user back.
    public ReactiveCommand<Unit, IRoutableViewModel> GoBack => Router.NavigateBack;

    public MainViewModel()
    {
        // Manage the routing state. Use the Router.Navigate.Execute
        // command to navigate to different view models.
        //
        // Note, that the Navigate.Execute method accepts an instance
        // of a view model, this allows you to pass parameters to
        // your view models, or to reuse existing view models.
        //
        GoNext = ReactiveCommand.CreateFromObservable(
            () =>
            {
                if (Router.NavigationStack.Count == 0)
                {
                    return Router.Navigate.Execute(new PageViewModel1(this));
                }
                else
                {
                    return Router.Navigate.Execute(new PageViewModel2(this) { MyProperty = "Page View " + (Router.NavigationStack.Count + 1)});
                }
            }
        );
    }
}
</file>

<file path="Samples/ReactiveSample/ViewModels/PageViewModel1.cs">
using ReactiveUI;
using ReactiveUI.SourceGenerators;

namespace ReactiveSample.ViewModels;

public partial class PageViewModel1 : ReactiveObject, IRoutableViewModel
{
    [Reactive]
    public partial string MyProperty { get; set; } = "Page View1";

    public IScreen HostScreen { get; }

    public string? UrlPathSegment => "page";

    public PageViewModel1(IScreen screen)
    {
        HostScreen = screen;
    }
}
</file>

<file path="Samples/ReactiveSample/ViewModels/PageViewModel2.cs">
using ReactiveUI;
using ReactiveUI.SourceGenerators;

namespace ReactiveSample.ViewModels;

public partial class PageViewModel2 : ReactiveObject, IRoutableViewModel
{
    [Reactive]
    public partial string MyProperty { get; set; } = "Page View2";

    public IScreen HostScreen { get; }

    public string? UrlPathSegment => "page";

    public PageViewModel2(IScreen screen)
    {
        HostScreen = screen;
    }
}
</file>

<file path="Samples/ReactiveSample/Views/MainView.cs">
using Avalonia.Styling;
using ReactiveSample.ViewModels;
using ReactiveUI.Avalonia;
using System.Reactive.Linq;

namespace ReactiveSample.Views;

internal class MainView : ReactiveViewBase<MainViewModel>
{
    protected override object Build(MainViewModel vm) =>
        new Grid()
            .Rows("*, Auto")
            .Children([
                new RoutedViewHost
                    {
                        ViewLocator = new AppViewLocator()
                    }
                    .DefaultContent(
                        new TextBlock()
                            .ReactiveBinding(TextBox.TextProperty, vm, x => x.MyProperty)
                            .HorizontalAlignment(HorizontalAlignment.Right)
                            .VerticalAlignment(VerticalAlignment.Center)
                    )
                    .Router(vm.Router)
                    .Row(0),
                new StackPanel()
                    .Orientation(Orientation.Horizontal)
                    .Margin(new Thickness(15))
                    .Styles([
                        new Style(x => x.OfType<StackPanel>().Child().OfType<Control>())
                            .Setter(MarginProperty, new Thickness(2)),
                        new Style(x => x.OfType<StackPanel>().Child().OfType<TextBlock>())
                            .Setter(VerticalAlignmentProperty, VerticalAlignment.Center),
                    ])
                    .Row(1)
                    .Children([
                        new Button()
                            .Content("Go back")
                            .Command(vm.GoBack),
                        new Button()
                            .Content("Go next")
                            .Command(vm.GoNext),
                        new TextBlock()
                            .Padding(5)
                            .ReactiveBinding(TextBlock.TextProperty, vm, x => x.Router.NavigationStack.Count, null, x => x.ToString())
                    ])
            ]);
}
</file>

<file path="Samples/ReactiveSample/Views/PageView1.cs">
using Avalonia.Styling;
using ReactiveSample.ViewModels;

namespace ReactiveSample.Views;

internal class PageView1 : ReactiveViewBase<PageViewModel1>
{
    protected override StyleGroup? BuildStyles() =>
    [
         new Style(x => x.Is<TextBlock>())
             .Setter(Control.HorizontalAlignmentProperty, HorizontalAlignment.Center)
             .Setter(Control.VerticalAlignmentProperty, VerticalAlignment.Center),
         new Style(x => x.Is<TextBox>())
             .Setter(Control.HorizontalAlignmentProperty, HorizontalAlignment.Center)
             .Setter(Control.VerticalAlignmentProperty, VerticalAlignment.Center),
    ];

    protected override object Build(PageViewModel1 vm) =>
        new StackPanel()
            .Children([
                new TextBlock()
                    .ReactiveBinding(TextBox.TextProperty, vm, x => x.MyProperty),
                new TextBox()
                    .ReactiveBinding(TextBox.TextProperty, vm, x => x.MyProperty, x => vm.MyProperty = x ?? ""),
                new TextBox()
                    .ReactiveBinding(TextBox.TextProperty, vm, x => x.MyProperty, x => vm.MyProperty = x ?? ""),
            ]);
}
</file>

<file path="Samples/ReactiveSample/Views/PageView2.cs">
using Avalonia.Markup.Declarative;
using Avalonia.Styling;
using ReactiveSample.ViewModels;
using ReactiveUI;
using System.Reactive.Disposables;

namespace ReactiveSample.Views;

internal class PageView2 : ReactiveViewBase<PageViewModel2>
{
    private TextBlock tb1 = null!;
    private TextBox tb2 = null!;
    private TextBox tb3 = null!;

    protected override StyleGroup? BuildStyles() =>
    [
         new Style(x => x.Is<TextBlock>())
             .Setter(Control.HorizontalAlignmentProperty, HorizontalAlignment.Center)
             .Setter(Control.VerticalAlignmentProperty, VerticalAlignment.Center),
         new Style(x => x.Is<TextBox>())
             .Setter(Control.HorizontalAlignmentProperty, HorizontalAlignment.Center)
             .Setter(Control.VerticalAlignmentProperty, VerticalAlignment.Center),
    ];

    protected override object Build(PageViewModel2 vm)
    {
        var controls = new StackPanel()
            .Children([
                new TextBlock()
                    .Ref(out tb1),
                new TextBox()
                    .Ref(out tb2),
                new TextBox()
                    .Ref(out tb3),
            ]);

        this.WhenActivated(disposable =>
        {
            disposable.Add(this.OneWayBind(vm, x => x.MyProperty, x => x.tb1.Text));
            disposable.Add(this.Bind(vm, x => x.MyProperty, x => x.tb2.Text));
            disposable.Add(this.Bind(vm, x => x.MyProperty, x => x.tb3.Text));
        });

        return controls;
    }
}
</file>

<file path="Samples/ReactiveSample/Views/ReactiveViewBase.cs">
using ReactiveUI;
using System.Diagnostics.CodeAnalysis;

namespace ReactiveSample.Views;

public abstract class ReactiveViewBase<TViewModel> : ViewBase, IViewFor<TViewModel> where TViewModel : class
{
    private object? _cachedDataContext;

    [SuppressMessage("AvaloniaProperty", "AVP1002", Justification = "Generic avalonia property is expected here.")]
    public static readonly StyledProperty<TViewModel?> ViewModelProperty = AvaloniaProperty.Register<ReactiveViewBase<TViewModel>, TViewModel?>(nameof(ViewModel));

    public TViewModel? ViewModel
    {
        get { return GetValue(ViewModelProperty); }
        set { SetValue(ViewModelProperty, value); }
    }

    object? IViewFor.ViewModel { get; set; }

    protected override void OnDataContextChanged(EventArgs e)
    {
        base.OnDataContextChanged(e);

        if (DataContext != null)
        {
            Initialize();
        }
    }


    protected abstract object Build(TViewModel vm);

    protected override object Build() => Build((TViewModel)(_cachedDataContext ?? DataContext!));

    protected override void OnBeforeReload()
    {
        _cachedDataContext = DataContext;
    }

    protected override void OnPropertyChanged(AvaloniaPropertyChangedEventArgs change)
    {
        base.OnPropertyChanged(change);

        if (change.Property == DataContextProperty)
        {
            if (ReferenceEquals(change.OldValue, ViewModel) && change.NewValue is null or TViewModel)
            {
                SetCurrentValue(ViewModelProperty, change.NewValue);
            }
        }
        else if (change.Property == ViewModelProperty)
        {
            if (ReferenceEquals(change.OldValue, DataContext))
            {
                SetCurrentValue(DataContextProperty, change.NewValue);
            }
        }
    }
}
</file>

</files>
